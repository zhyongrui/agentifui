## P3-C-03 Shared conversation viewer identity chips

Completed `P3-C-03` by extending presence onto shared conversation surfaces and rendering collaborator identity chips on the read-only shared transcript page.

Implemented in this round:

- extended presence surface contracts in:
  - `packages/shared/src/apps/contracts.ts`
  - added `shared_conversation` to `WorkspaceConversationPresenceSurface`
- added shared presence routes in:
  - `apps/gateway/src/routes/workspace.ts`
  - new endpoints:
    - `GET /workspace/shares/:shareId/presence`
    - `PUT /workspace/shares/:shareId/presence`
- wired shared presence lookup/update in:
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - these routes reuse the same TTL-pruned per-conversation presence map as owner surfaces
- added shared presence client helpers in:
  - `apps/web/src/lib/apps-client.ts`
  - `fetchWorkspaceSharedConversationPresence(...)`
  - `updateWorkspaceSharedConversationPresence(...)`
- updated the shared transcript UI in:
  - `apps/web/src/app/(main)/chat/shared/[shareId]/page.tsx`
  - shared pages now:
    - keep a stable `sessionStorage` presence id keyed by `shared:<shareId>`
    - heartbeat with `surface = shared_conversation`
    - render collaborator identity chips with viewer state and surface labels
    - surface live-sync counts and last-sync timestamp in the header

Continuity notes:

- shared and owner presence intentionally share one underlying conversation presence set
  - chips can therefore show both `owner` and `shared` viewers when the same conversation is open from multiple surfaces
- the shared page reuses the same 10-second heartbeat cadence as the owner conversation page
  - there is still no push transport; future cursor/comment work should build on this instead of adding a second polling loop
- shared presence routes are authorization-scoped by share membership
  - a user who can open the shared transcript can also contribute a shared-surface presence heartbeat, but cannot write to the owner conversation route

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t "tracks shared conversation presence sessions for shared viewers|tracks conversation presence sessions for multiple viewers"`
- `npx vitest run apps/web/src/lib/apps-client.test.ts apps/web/src/lib/workspace-presence.test.ts`
- `npm run build`

Observed results:

- type-check passed
- shared presence route tests passed
- web client + helper tests passed
- production build passed
