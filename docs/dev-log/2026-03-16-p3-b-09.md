# 2026-03-16 P3-B-09 Tool Call Summaries In Transcript And Replay

## Summary

- replaced the ad-hoc tool-call lists with a shared summary component for transcript and replay surfaces
- added localized tool execution metadata chips for attempt, latency, timeout budget, failure reason, and idempotency key
- reused the same summary rendering on the shared transcript surface so tool-call replay copy stays consistent

## Files

- `apps/web/src/components/workspace-tool-summary.tsx`
- `apps/web/src/components/workspace-tool-summary.test.tsx`
- `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
- `apps/web/src/app/(main)/chat/shared/[shareId]/page.tsx`
- `apps/web/src/app/globals.css`
- `docs/plans/PHASE2_EXECUTION_PLAN.md`

## Validation

- `npm run type-check`
- `npx vitest run apps/web/src/components/workspace-tool-summary.test.tsx`
- `npm run build`

## Observed Results

- `type-check` passed
- the new component test passed with `2` tests
- production build passed

## Continuity Notes

- `WorkspaceToolCallSummaryList` and `WorkspaceToolExecutionSummaryList` intentionally summarize structured data, not raw markdown
  - transcript bubbles should keep using the typed `toolCalls` / `toolExecutions` fields
  - do not rebuild these summaries by parsing assistant text
- the same summary component is now reused by:
  - `/chat/[conversationId]`
  - `/chat/shared/[shareId]`
  - run replay in the conversation detail page
- the component test uses `renderToStaticMarkup(...)`
  - on this repo, JSX files used by that path still need an explicit `import React from "react"`
  - otherwise Vitest fails with `React is not defined`
