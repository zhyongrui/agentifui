# 2026-03-16 P3-A-11

Completed `P3-A-11` by extending workspace cleanup to cover stale knowledge sources.

What changed:

- cleanup policy now includes `staleKnowledgeSourceRetentionDays = 30`
- cleanup preview/summary now include:
  - `staleKnowledgeSources`
  - `staleKnowledgeSourcesDeleted`
  - `staleKnowledgeSourceBefore`
- stale candidates are sources older than the cutoff where:
  - status is `queued`, `processing`, or `failed`
  - or status is `succeeded` but effectively empty (`chunk_count = 0` or `source_content is null`)
- admin apps cleanup cards now surface stale-source counts

Validation:

- `npm run type-check`
- `npx vitest run apps/gateway/src/services/workspace-cleanup.test.ts apps/gateway/src/routes/admin.test.ts apps/gateway/src/routes/knowledge-persistence.test.ts`
- `npm run build`

Continuity notes:

- stale-source deletion goes through `knowledge_sources`; chunk cleanup is handled by FK cascade
- the new persistence test lives in `apps/gateway/src/routes/knowledge-persistence.test.ts`, not the older giant persistence file
