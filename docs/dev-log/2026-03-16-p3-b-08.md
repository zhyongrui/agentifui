# 2026-03-16 P3-B-08 Admin Tool Policy Controls

## Summary

- exposed tool execution policy controls on the tenant admin apps page
- persisted tool timeout / retry / idempotency overrides in the existing registry override table
- kept the legacy `enabledToolNames` update payload working while adding the richer `tools[]` form
- verified the persisted override affects runtime tool execution after restart

## Files

- `packages/shared/src/admin/contracts.ts`
- `packages/shared/src/tools/contracts.ts`
- `packages/db/src/schema/workspace.ts`
- `packages/db/migrations/0015_p3_b8_tool_policy_overrides.sql`
- `apps/gateway/src/services/tool-registry-service.ts`
- `apps/gateway/src/services/persistent-tool-registry-service.ts`
- `apps/gateway/src/routes/admin.ts`
- `apps/gateway/src/routes/admin.test.ts`
- `apps/gateway/src/routes/tool-registry-persistence.test.ts`
- `apps/web/src/app/admin/apps/page.tsx`
- `apps/web/src/app/globals.css`
- `apps/web/src/lib/i18n.ts`
- `apps/web/src/lib/admin-client.test.ts`
- `docs/plans/PHASE2_EXECUTION_PLAN.md`

## Validation

- `npm run type-check`
- `npm run build`
- `npx vitest run apps/gateway/src/routes/admin.test.ts apps/gateway/src/routes/tool-registry-persistence.test.ts apps/web/src/lib/admin-client.test.ts`

## Observed Results

- `type-check` passed
- `build` passed
- targeted Vitest: `30` tests passed

## Continuity Notes

- `workspace_app_tool_overrides` is no longer “enabled only”
  - it now stores the effective override values for:
    - `timeout_ms`
    - `max_attempts`
    - `idempotency_scope`
- policy-only overrides still create a row
  - do not interpret row existence as “enabled changed”
  - use:
    - `enabledIsOverridden`
    - `executionIsOverridden`
    - `isOverridden`
- the admin page saves the richer `tools[]` payload, but the route still accepts `enabledToolNames`
  - this keeps older scripts/tests working during the transition
- the persistence test now proves the runtime reads admin policy overrides after restart
  - no custom `tools` payload is passed on the chat request
  - the run metadata is enough to verify the override took effect
