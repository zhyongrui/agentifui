# 2026-03-16 P3-B-07 Tool Timeout Retry And Idempotency

## Summary

- added explicit tool execution policy fields to the shared tool contract
- seeded default timeout/retry/idempotency settings for the built-in tool catalog
- made placeholder runtime attempts replayable with per-attempt metadata
- kept approval-gated tools on the same metadata path
- verified conversation-scoped idempotency survives app restarts

## Files

- `packages/shared/src/tools/contracts.ts`
- `packages/shared/src/apps/contracts.ts`
- `apps/gateway/src/services/tool-registry-service.ts`
- `apps/gateway/src/services/workspace-runtime.ts`
- `apps/gateway/src/services/workspace-tool-approval.ts`
- `apps/gateway/src/routes/chat.ts`
- `apps/gateway/src/services/workspace-service.ts`
- `apps/gateway/src/services/persistent-workspace-service.ts`
- `apps/gateway/src/routes/chat.test.ts`
- `apps/gateway/src/routes/auth-persistence.test.ts`
- `docs/plans/PHASE2_EXECUTION_PLAN.md`

## Validation

- `npm run type-check`
- `npm run build`
- `npx vitest run apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t "retry|timeout|idempot|typed tool|approval-required tool|runtime tool calls and tool result messages|conversation-scoped tool idempotency"`

## Observed Results

- `type-check` passed
- `build` passed
- targeted Vitest: `5` tests passed, `40` skipped

## Continuity Notes

- `inputs.toolSimulation` is the deterministic retry/timeout harness for the placeholder runtime
  - `failAttemptsBeforeSuccess`
  - `alwaysFail`
  - `alwaysTimeout`
- conversation-scoped idempotency is based on:
  - `conversation.id`
  - tool name
  - serialized tool arguments
- run-scoped idempotency swaps `conversation.id` for `run.id`
- audit payloads now include the same execution `metadata` map that replay surfaces use
  - this is the fastest path when debugging timeout-heavy test failures through `/admin/audit`
- persistence tests that expect password login should use non-SSO domains
  - `iflabx.com` still routes through enterprise-domain behavior in the test environment
