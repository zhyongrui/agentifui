# 2026-03-16 P3-C-08 Comment Audit Coverage

## Summary

- added `workspace.comment.created` to the shared audit action contract
- recorded structured audit events from the new workspace comment create route
- verified audit persistence for message/run/artifact comments in both memory and persistent apps

## Files

- `packages/shared/src/auth/contracts.ts`
- `apps/gateway/src/routes/workspace.ts`
- `apps/gateway/src/routes/workspace.test.ts`
- `apps/gateway/src/routes/auth-persistence.test.ts`
- `docs/plans/PHASE2_EXECUTION_PLAN.md`
- `docs/dev-log/2026-03-16.md`

## Validation

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t 'creates workspace comment threads for messages, runs and artifacts'`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t 'persists workspace comments for messages, runs and artifacts across app restarts'`

## Observed Results

- `type-check` passed
- memory route coverage passed with audit assertions
- persistence restart coverage passed with audit assertions

## Continuity Notes

- comment audit uses:
  - `action = workspace.comment.created`
  - `entityType = conversation_comment`
- audit payload now carries:
  - `conversationId`
  - `targetType`
  - `targetId`
  - `threadLength`
- the comment route is the only write path today
  - future shared-surface write support or mention notifications should reuse the same audit action family instead of inventing a parallel one
