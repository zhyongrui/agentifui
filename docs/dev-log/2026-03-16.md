# 2026-03-16

## P3-A-03 Knowledge Source Chunking Strategy

Completed `P3-A-03` by choosing concrete chunking profiles for source ingestion, persisting the chunking metadata, storing normalized source content for later retrieval work, and exposing the strategy in the admin source workflow.

Implemented in this round:

- shared contracts:
  - `packages/shared/src/knowledge/contracts.ts`
  - added:
    - `KnowledgeChunkingStrategy`
    - `KnowledgeSourceChunking`
    - `KnowledgeSourceChunk`
    - `KnowledgeSource.hasContent`
    - `KnowledgeSource.chunking`
    - `KnowledgeSourceCreateRequest.content`
    - `KnowledgeSourceStatusUpdateRequest.content`
    - `KnowledgeSourceChunksResponse`
- gateway chunking service:
  - `apps/gateway/src/services/knowledge-chunking.ts`
  - profiles now default to:
    - `markdown -> markdown_sections`
    - `url/file -> paragraph_windows`
  - markdown-like headings inside non-markdown sources also promote the strategy to `markdown_sections`
  - chunk records now carry:
    - `headingPath`
    - `preview`
    - `charCount`
    - `tokenEstimate`
- database and persistence:
  - migration:
    - `packages/db/migrations/0013_p3_a_knowledge_chunking.sql`
  - schema:
    - `packages/db/src/schema/workspace.ts`
    - `knowledge_sources` now stores:
      - `source_content`
      - `chunking_strategy`
      - `chunk_target_chars`
      - `chunk_overlap_chars`
      - `last_chunked_at`
    - new `knowledge_source_chunks` table
  - schema exports:
    - `packages/db/src/index.ts`
  - backup allowlist:
    - `scripts/lib/backup-helpers.mjs`
    - now includes:
      - `knowledge_sources`
      - `knowledge_source_chunks`
- gateway knowledge services:
  - `apps/gateway/src/services/knowledge-service.ts`
    - in-memory ingestion now recomputes chunking on create/update when content is supplied
  - `apps/gateway/src/services/persistent-knowledge-service.ts`
    - persisted create/update now:
      - store normalized content
      - store chunking metadata
      - replace per-source chunk rows
  - `apps/gateway/src/routes/admin-sources.ts`
    - create/status-update requests now accept `content`
- web/admin:
  - `apps/web/src/app/admin/sources/page.tsx`
    - queue form now includes source-content textarea
    - source cards now show:
      - content-presence flag
      - chunk strategy
      - target size
      - overlap
      - last chunked timestamp

Useful continuity notes for future sessions:

- chunking is intentionally heuristic-first for now
  - markdown sources always use `markdown_sections`
  - `url` and `file` sources use `paragraph_windows` unless the content visibly contains markdown headings
  - retrieval ranking and semantic search are not implemented yet; this round only prepares source material for those later slices
- `knowledge_source_chunks` currently stores raw chunk text and preview text
  - later retrieval work should read from this table instead of reparsing `knowledge_sources.source_content`
  - if a future migration changes chunk shape, remember to update:
    - `replaceSourceChunks(...)`
    - backup allowlist in `scripts/lib/backup-helpers.mjs`
- `P3-A-03` does not add any new browser coverage
  - browser source-management coverage remains a later task in `P3-A-10`
  - persistence-heavy ingestion transition coverage remains a later task in `P3-A-09`
- large-file patch safety remains relevant on this host
  - avoid big `apply_patch` hunks on:
    - `apps/gateway/src/services/persistent-workspace-service.ts`
    - `apps/gateway/src/routes/chat.ts`
    - `apps/gateway/src/services/knowledge-service.ts`
  - if a timed-out patch truncates a file, recover with:
    - `git show HEAD:path/to/file > path/to/file`

Validation performed in this round:

- `npm run type-check`
- targeted Vitest:
  - `npx vitest run apps/gateway/src/services/knowledge-chunking.test.ts apps/gateway/src/routes/admin-sources.test.ts apps/web/src/lib/admin-client.test.ts`
- `npm run build`

Observed results:

- `type-check` passed across shared/db/gateway/web
- targeted Vitest passed with:
  - `3` files
  - `12` tests
- production build passed and continues to include:
  - `/admin/sources`

Next active item:

- `P3-A-05` retrieval query abstraction usable by runtime adapters

## Web I18n Baseline

Completed a first-pass web i18n baseline so the UI defaults to Simplified Chinese while still allowing runtime language switching between `zh-CN` and `en-US`.

Implemented in this round:

- new client i18n infrastructure:
  - `apps/web/src/lib/i18n.ts`
  - `apps/web/src/components/i18n-provider.tsx`
  - `apps/web/src/components/language-switcher.tsx`
- root/layout wiring:
  - `apps/web/src/app/layout.tsx`
  - `apps/web/src/app/(main)/layout.tsx`
  - `apps/web/src/app/(auth)/layout.tsx`
  - `apps/web/src/app/admin/layout.tsx`
- translated primary navigation and main admin surfaces:
  - `apps/web/src/components/main-section-nav.tsx`
  - `apps/web/src/components/admin-section-nav.tsx`
  - `apps/web/src/app/(auth)/login/page.tsx`
  - `apps/web/src/app/(auth)/register/page.tsx`
  - `apps/web/src/app/(main)/apps/page.tsx`
  - `apps/web/src/app/admin/apps/page.tsx`
  - `apps/web/src/app/admin/users/page.tsx`
  - `apps/web/src/app/admin/groups/page.tsx`
- shared styling for the language switcher and layout toolbar:
  - `apps/web/src/app/globals.css`
- browser-suite stabilization after the locale default change:
  - `tests/e2e/phase1-flows.spec.ts`
  - `tests/e2e/knowledge-flows.spec.ts`

Useful continuity notes for future sessions:

- the default locale is intentionally `zh-CN`
  - the browser switcher persists to `localStorage["agentifui.locale"]`
  - tests pin `en-US` explicitly with `page.addInitScript(...)` so selector-heavy browser coverage does not flap on text changes
- auth redirects now use `window.location.assign(...)` instead of `router.push(...)`
  - on this codebase, preloading `en-US` before hydration caused register/login success flows to intermittently stay on the auth route during Playwright runs
  - the full-page redirect is the pragmatic fix and still preserves `sessionStorage` for same-origin auth state
- partial localization is in place, not total localization
  - auth, workspace landing, admin users/groups/apps, and shared nav are translated
  - larger secondary surfaces like chat detail, audit, tenants, and sources still need later passes if Chinese coverage is meant to be exhaustive
- this host still has browser-runtime resource pressure
  - local targeted browser smoke passed
  - full `npm run test:e2e` still reproduced `net::ERR_INSUFFICIENT_RESOURCES` noise and stopped early on the first auth/browser path
  - if that suite is revisited, reduce parallel browser/process load first and prefer isolated reruns

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- targeted browser smoke with Playwright runtime libs:
  - `i18n-login-switch-smoke:ok`
  - `i18n-login-apps-smoke:ok`
- attempted full browser suite:
  - `npm run test:e2e`

Observed results:

- `type-check` passed
- production build passed
- targeted browser smoke confirmed:
  - login defaults to Chinese
  - switching to English updates the auth copy
  - Chinese login still lands on `/apps`
- full `test:e2e` did not finish green on this host
  - the suite still hit browser resource exhaustion and exited early

## Browser localization follow-up

Why there was still a lot of English after the first i18n pass:

- only auth, main nav, `/apps`, and part of admin had been wired to locale-aware copy
- app catalog seed data itself still used English `name/summary/kind/status/tags`
- chat surfaces (`/chat`, `/chat/[conversationId]`, `/chat/shared/[shareId]`) still had many hard-coded English labels

What changed in this follow-up:

- added `apps/web/src/lib/workspace-localization.ts`
  - localizes seeded workspace app `name` and `summary`
  - localizes app `kind`, `status`, and common tags for `zh-CN`
- wired the helper into:
  - `apps/web/src/app/(main)/apps/page.tsx`
  - `apps/web/src/app/admin/apps/page.tsx`
  - `apps/web/src/app/(main)/chat/page.tsx`
  - `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
  - `apps/web/src/app/(main)/chat/shared/[shareId]/page.tsx`
- added a larger second-pass Chinese copy layer for the most visible chat/history/shared labels and actions

Browser environment continuity notes:

- the current public manual-test URL still points to the fresh `3130` web process:
  - `https://distinguished-subsidiary-josh-watches.trycloudflare.com`
- after rebuilding, the `3130` Next.js process was restarted in place
  - the tunnel URL did not need to change
- if the browser still shows stale English after this pass, do a hard refresh first
  - the tunnel can serve cached HTML shells and the browser may keep older chunks briefly

Validation performed in this follow-up:

- `npm run build`
- `npm run type-check`

Observed results:

- `build` passed
- `type-check` passed after rerunning post-build
- the earlier `.next/types/routes.d.ts` miss was a generation timing issue, not a persistent typing failure

## Secondary localization pass and Next type-check fix

This follow-up continued the Chinese-default pass beyond the first visible workspace/chat surfaces.

Implemented in this round:

- localized more high-visibility pages:
  - `apps/web/src/app/admin/audit/page.tsx`
  - `apps/web/src/app/admin/sources/page.tsx`
  - `apps/web/src/app/admin/tenants/page.tsx`
  - `apps/web/src/app/(main)/chat/artifacts/[artifactId]/page.tsx`
- kept the approach page-local and display-only
  - API contracts and persistence models stayed unchanged
  - visible labels, buttons, helper copy, empty states, and export notices now default to Chinese while still allowing `en-US`

Next.js 16 type-check continuity note:

- `@agentifui/web` type-check can fail after `next typegen` with:
  - `.next/types/validator.ts` importing `./routes.js`
  - but Next only emitting `routes.d.ts`
- direct manual workaround proved the issue was not app code:
  - copying `.next/types/routes.d.ts` to `.next/types/routes.ts` makes `tsc --noEmit` pass
- the durable fix is now in:
  - `apps/web/scripts/fix-next-typegen.mjs`
  - `apps/web/package.json`
- important implementation detail:
  - do not resolve from `process.cwd()` under `turbo`
  - resolve from `import.meta.url` / script directory instead, otherwise the file gets copied into the wrong folder and the fix silently fails

Validation performed in this round:

- `npm run type-check`
- `npm run build`

Observed results:

- `type-check` passed after wiring the Next typegen compatibility script
- `build` passed with the secondary localization changes

## P3-B-03 runtime tool call support

This follow-up closed the first runtime tool invocation slice.

Implemented in this round:

- extended workspace transcript messages to support:
  - assistant `toolCalls`
  - persisted `tool` role messages
  - `toolCallId` / `toolName`
- added placeholder runtime tool execution for explicit `tools` / function `tool_choice`
  - the runtime now synthesizes deterministic tool calls and tool result messages
  - ordinary no-tool chat requests still stay on the prior path
- updated chat persistence and reload behavior
  - conversation history now reloads assistant tool-request messages, tool result messages, and the final assistant reply after restart
- updated the chat/shared transcript UI
  - tool bubbles render with a dedicated style
  - assistant bubbles can show a lightweight tool-call list

Continuity notes:

- the current persisted ordering is:
  - request messages from `body.messages`
  - runtime-generated assistant tool-call message
  - runtime-generated tool message(s)
  - final assistant reply
- the common write point is `apps/gateway/src/routes/chat.ts -> buildPersistedHistory()`
  - future latency / retry / approval work for `P3-B-04+` should extend this helper instead of writing custom transcript order
- browser streaming now accepts `delta.tool_calls`
  - the live UI only renders a lightweight list for now
  - fuller transcript/replay summaries are still reserved for `P3-B-09`

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- `npx vitest run apps/gateway/src/routes/chat.test.ts`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t 'persists runtime tool calls and tool result messages across app restarts'`

Observed results:

- `type-check` passed
- `build` passed
- `chat.test.ts`: `16` tests passed
- targeted persistence: `1` test passed

## P3-B-04 tool execution run boundary

This follow-up normalized tool-stage replay data into a stable run-boundary model.

Implemented in this round:

- added `WorkspaceRun.toolExecutions`
  - each entry now carries:
    - request payload
    - stored result payload
    - attempt number
    - status
    - latency
- kept backward compatibility with older run payloads
  - read paths still derive `toolExecutions` from legacy `outputs.toolCalls` / `outputs.toolResults` when needed
- updated run replay UI with a minimal tool execution section
- stabilized the root `npm run type-check` path on this host
  - non-web packages still go through `turbo`
  - `@agentifui/web` type-check now runs serially afterward to avoid the flaky Next 16 `.next/types/routes.ts` race

Continuity notes:

- the current tool latency written by `chat.ts` is captured when the runtime returns
  - this is intentionally tool-stage latency, not total streamed reply latency
  - future real runtimes should overwrite this with actual per-tool measurements
- legacy fallback timestamps for old runs use persisted run timestamps instead of `new Date()` on read
  - this keeps replay output stable across reloads
- replay UI intentionally stops at a minimal summary
  - richer transcript/run summaries still belong to `P3-B-09`

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- `npx vitest run apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t 'tool'`

Observed results:

- `type-check` passed after serializing the root web type-check path
- `build` passed
- targeted Vitest: `5` tests passed

## P3-B-05 approval-required tool execution paths

This follow-up reused the existing HITL boundary to gate selected tool calls behind an explicit approval step.

Implemented in this round:

- added `WorkspaceHitlStep.metadata`
  - tool-approval steps now carry:
    - `kind = tool_approval`
    - `toolCallId`
    - `toolName`
    - serialized tool arguments
    - optional `policyTag`
- marked `tenant.access.review` as the first approval-required seeded tool
- updated the placeholder runtime
  - explicit `tool_choice` requests for approval-gated tools now return:
    - assistant `tool_calls`
    - a pending approval step
    - no immediate `toolResults` / `toolExecutions`
- updated pending-action response handling in both memory and persistent workspace services
  - approving/rejecting/cancelling a tool-approval step now appends:
    - a persisted `tool` transcript message
    - a persisted assistant follow-up message
    - `outputs.toolResults`
    - `outputs.toolExecutions`
    - an additional `output_recorded` timeline event
- updated the chat page pending-action response flow
  - after a response, the page now reloads the conversation and selected run from the server instead of only patching local `pendingActions`
- tightened legacy replay fallback
  - derived tool executions now ignore `toolCalls` that have no matching result payload
  - this prevents approval-pending runs from incorrectly rendering as already-succeeded executions

Continuity notes:

- the tool-approval transport is currently `WorkspaceHitlStep.metadata`
  - future runtimes should reuse the same metadata keys instead of inventing a second approval envelope
- the approval resolution currently mutates the same run that requested the tool
  - transcript replay therefore shows:
    - the initial assistant tool-call request
    - the later tool result
    - the later assistant approval-resolution follow-up
- if the frontend only patches `pendingActions` locally, replay goes stale
  - the required post-response behavior is now:
    - reload conversation
    - reload run detail
    - prefer the returned `runId`
- `tenant.access.review` is now the seeded approval-required path for tests
  - use that tool for browser/API validation until richer real tools exist

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- `npx vitest run apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/workspace.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t "approval|required tool|records transcript and tool execution details|persists approval-required tool responses across app restarts"`

Observed results:

- `type-check` passed
- `build` passed
- targeted Vitest: `4` tests passed, `59` skipped

## P3-B-06 tool execution and approval audit coverage

This follow-up closed the missing audit plane around tool execution and operator approval decisions.

Implemented in this round:

- extended the shared auth audit action enum with tool-stage actions:
  - `workspace.tool_execution.approval_requested`
  - `workspace.tool_execution.approval_decided`
  - `workspace.tool_execution.completed`
  - `workspace.tool_execution.failed`
  - `workspace.tool_execution.blocked`
- added chat-route audit coverage
  - immediate tool runs now emit execution audit events
  - approval-gated tool requests now emit `approval_requested`
- added workspace pending-action audit coverage
  - tool approval responses now emit:
    - `approval_decided`
    - `completed` on approve
    - `blocked` on reject/cancel
- added route and persistence coverage
  - in-memory tests now verify both execution and approval audit events
  - persistence coverage now verifies the new events through `/admin/audit`

Continuity notes:

- `workspace.pending_action.responded` still exists and remains the generic HITL record
  - the new tool-specific audit events are additive, not replacements
- the canonical tool-approval payload still comes from `WorkspaceHitlStep.metadata`
  - audit payloads derive `toolCallId`, `toolName`, and `policyTag` from that metadata
- admin audit filtering for the new events works through the existing `action=` query parameter
  - use `traceId=` alongside `action=` when you need to isolate a single tool approval run in persistence tests

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- `npx vitest run apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/workspace.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t "tool_execution|approval|required tool|records transcript and tool execution details|persists approval-required tool responses across app restarts|accepts typed tool descriptors"`

Observed results:

- `type-check` passed
- `build` passed
- targeted Vitest: `5` tests passed, `58` skipped

## P3-B-07 tool timeout, retry, and idempotency handling

This follow-up closed the missing execution-policy layer for tool calls and made the behavior replayable across retries and restarts.

Implemented in this round:

- extended the shared tool contract
  - `packages/shared/src/tools/contracts.ts`
  - added `execution` policy with:
    - `timeoutMs`
    - `maxAttempts`
    - `idempotencyScope`
- extended run replay contracts
  - `packages/shared/src/apps/contracts.ts`
  - `WorkspaceRunToolExecution` now carries optional `metadata`
- updated seeded tool registry defaults
  - `apps/gateway/src/services/tool-registry-service.ts`
  - seeded tools now declare explicit execution policies so retry/timeout behavior is not hidden in runtime internals
- updated placeholder runtime execution flow
  - `apps/gateway/src/services/workspace-runtime.ts`
  - tool simulation now supports:
    - retry-before-success
    - timeout-only failure loops
    - stable idempotency key generation
  - each attempt now records:
    - `attempt`
    - `startedAt`
    - `finishedAt`
    - `latencyMs`
    - `metadata.failureReason`
    - `metadata.idempotencyKey`
    - `metadata.maxAttempts`
    - `metadata.timeoutMs`
- updated approval metadata propagation
  - `apps/gateway/src/services/workspace-tool-approval.ts`
  - approval-gated tools now preserve timeout/idempotency metadata on the HITL step and on the eventual tool execution record
- updated replay readers and audit payloads
  - `apps/gateway/src/routes/chat.ts`
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - multiple attempts now survive both in-memory and persisted replay
  - tool execution audit payloads now include the execution `metadata` map

Continuity notes:

- deterministic tool retry/timeout coverage currently comes from `inputs.toolSimulation`
  - this is a placeholder-runtime test hook, not a user-facing product feature
  - supported knobs today are:
    - `failAttemptsBeforeSuccess`
    - `alwaysFail`
    - `alwaysTimeout`
- idempotency keys are intentionally derived from scope + tool name + serialized arguments
  - `conversation` scope uses `conversation.id`
  - `run` scope uses `run.id`
  - retries inside the same run keep the same idempotency key
  - repeated conversation-scoped calls with the same arguments across later runs also keep the same key
- persistence tests that expect ordinary password login should avoid the enterprise SSO domain
  - `iflabx.com` is still mapped to enterprise SSO behavior in test env
  - use `example.net` for plain register/login flows unless the test is specifically about SSO
- auth route JSON shape in persistence tests remains `response.json().data.sessionToken`
  - using `response.json().sessionToken` silently produces bearer `undefined` and looks like a workspace auth failure later

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- targeted Vitest:
  - `npx vitest run apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t "retry|timeout|idempot|typed tool|approval-required tool|runtime tool calls and tool result messages|conversation-scoped tool idempotency"`

Observed results:

- `type-check` passed
- `build` passed
- targeted Vitest: `5` tests passed, `40` skipped

## P3-B-08 admin tool policy controls

This follow-up moved tool execution policy from a seeded backend-only concept onto the tenant admin surface.

Implemented in this round:

- extended admin/shared contracts
  - `packages/shared/src/admin/contracts.ts`
  - `packages/shared/src/tools/contracts.ts`
  - admin tool updates can now carry `tools[]` with:
    - `enabled`
    - `execution.timeoutMs`
    - `execution.maxAttempts`
    - `execution.idempotencyScope`
  - tool summaries now expose:
    - effective execution policy
    - default execution policy
    - per-axis override flags
- extended tool override persistence
  - `packages/db/src/schema/workspace.ts`
  - `packages/db/migrations/0015_p3_b8_tool_policy_overrides.sql`
  - `workspace_app_tool_overrides` now also stores:
    - `timeout_ms`
    - `max_attempts`
    - `idempotency_scope`
- updated memory + persistent tool registry services
  - `apps/gateway/src/services/tool-registry-service.ts`
  - `apps/gateway/src/services/persistent-tool-registry-service.ts`
  - admin writes now persist execution overrides, not just enable/disable state
  - runtime `getEnabledToolsForUser(...)` now returns descriptors with the overridden execution policy
- updated admin route and audit payload
  - `apps/gateway/src/routes/admin.ts`
  - the route still accepts legacy `enabledToolNames` updates for compatibility
  - the audit payload now records `toolPolicies`
- updated admin app UI
  - `apps/web/src/app/admin/apps/page.tsx`
  - each tool row now lets admins edit:
    - timeout
    - max attempts
    - idempotency scope
  - saved values round-trip through the same admin surface
- updated copy/styling
  - `apps/web/src/lib/i18n.ts`
  - `apps/web/src/app/globals.css`

Continuity notes:

- the override row now represents the full effective tenant override for a tool
  - enablement override only
  - execution-policy override only
  - or both together
- policy-only overrides intentionally keep `enabled = defaultEnabled`
  - that means a row can exist even when the tool remains enabled exactly as the catalog default
  - `isOverridden` should now be read as:
    - enabled override
    - execution override
    - or both
- the admin route remains backward compatible
  - legacy payload:
    - `enabledToolNames`
  - new payload:
    - `tools`
  - the web admin page now uses the richer `tools` payload
- the best persistence sanity check is:
  - save a tool policy override in `/admin/apps`
  - restart the app
  - run a chat completion without custom `tools`
  - confirm the resulting run metadata uses the overridden timeout/attempt values

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- targeted Vitest:
  - `npx vitest run apps/gateway/src/routes/admin.test.ts apps/gateway/src/routes/tool-registry-persistence.test.ts apps/web/src/lib/admin-client.test.ts`

Observed results:

- `type-check` passed
- `build` passed
- targeted Vitest: `30` tests passed
- browser coverage was not added or rerun in this round

## P3-B-09 Tool-call summaries in transcript and replay

Completed `P3-B-09` by replacing the old plain tool-call bullet lists with a shared summary component on the main conversation view, shared transcript view, and run replay panel.

Implemented in this round:

- added shared web summary rendering:
  - `apps/web/src/components/workspace-tool-summary.tsx`
  - includes:
    - `WorkspaceToolCallSummaryList`
    - `WorkspaceToolExecutionSummaryList`
  - summarizes:
    - tool name
    - call id / status
    - parsed arguments
    - attempt count
    - latency
    - timeout budget
    - max attempts
    - failure reason
    - idempotency key
    - result preview
- wired the component into:
  - `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
  - `apps/web/src/app/(main)/chat/shared/[shareId]/page.tsx`
- added styling for the new cards:
  - `apps/web/src/app/globals.css`
- added focused component coverage:
  - `apps/web/src/components/workspace-tool-summary.test.tsx`

Continuity notes:

- tool-call summaries should stay bound to structured data
  - transcript views should render from `message.toolCalls`
  - replay views should render from `run.toolExecutions`
  - do not fall back to parsing assistant markdown unless the contracts change
- the shared transcript and the main conversation surface now intentionally reuse the same component
  - if copy or layout changes later, update both through `workspace-tool-summary.tsx`
- the focused test uses `renderToStaticMarkup(...)`
  - this path still requires an explicit `import React from "react"` inside the JSX component module on this repo

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/web/src/components/workspace-tool-summary.test.tsx`
- `npm run build`

Observed results:

- `type-check` passed
- the focused component test passed with `2` tests
- `build` passed

## P3-B-10 Tool-stage failure taxonomy extensions

Completed `P3-B-10` by extending the structured failure taxonomy to failed tool executions and keeping backward-compatible fallback logic for older persisted rows that only have `metadata.failureReason`.

Implemented in this round:

- expanded the shared failure taxonomy in:
  - `packages/shared/src/apps/contracts.ts`
  - added tool-stage codes:
    - `tool_timeout`
    - `tool_provider_error`
    - `tool_approval_rejected`
    - `tool_approval_cancelled`
    - `tool_approval_expired`
  - added tool-stage stages:
    - `tool_execution`
    - `tool_approval`
  - `WorkspaceRunToolExecution` now supports a structured `failure` field
- added tool-failure derivation helpers in:
  - `apps/gateway/src/services/workspace-run-failure.ts`
  - failed tool executions can now derive structured failure objects from:
    - explicit persisted `failure`
    - or legacy `metadata.failureReason`
- wired the new failure payload into:
  - `apps/gateway/src/routes/chat.ts`
  - `apps/gateway/src/services/workspace-tool-approval.ts`
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - audit payloads now also include `execution.failure`
- updated replay rendering in:
  - `apps/web/src/components/workspace-tool-summary.tsx`
  - replay cards now show:
    - failure code
    - failure stage
    - failure message
    - retryability

Continuity notes:

- do not overload `run.failure` to describe every failed tool attempt
  - a run can still succeed while some tool attempts fail
  - those failures now belong on `toolExecutions[*].failure`
- new persisted tool execution rows should write both:
  - `metadata.failureReason` for old consumers
  - structured `failure` for new readers
- readers remain backward compatible
  - if `toolExecutions[*].failure` is missing, they derive it from legacy metadata and result content

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/services/workspace-run-failure.test.ts apps/web/src/components/workspace-tool-summary.test.tsx apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t "tool|persists runtime tool calls and tool result messages across app restarts"`
- `npm run build`

Observed results:

- `type-check` passed
- targeted Vitest passed with `4` files, `14` tests passed, `37` skipped
- `build` passed

## P3-B-11 Tool run matrix tests

Completed `P3-B-11` by filling the missing cancelled approval-gated tool-run coverage and verifying the existing success / failure / pending approval / approved execution paths as one focused matrix.

Implemented in this round:

- extended the workspace approval audit payload in:
  - `apps/gateway/src/routes/workspace.ts`
  - `workspace.tool_execution.blocked` and `workspace.tool_execution.completed` now include the matching tool execution `failure` when present
- upgraded the route-level cancelled approval test in:
  - `apps/gateway/src/routes/workspace.test.ts`
  - it now verifies:
    - explicit approval-gated tool invocation
    - cancelled pending action response
    - persisted tool transcript message
    - replayable failed tool execution with `tool_approval_cancelled`
    - blocked audit payload
- upgraded the restart/persistence cancel test in:
  - `apps/gateway/src/routes/auth-persistence.test.ts`
  - it now verifies the same cancelled tool execution survives app restart and remains queryable from `/workspace/runs/:runId` and `/admin/audit`

Continuity notes:

- the cancelled tool-run path only exists when the completion explicitly requests the approval-gated tool
  - use:
    - `tool_choice.type = function`
    - `tool_choice.function.name = tenant.access.review`
  - without that, the test only hits the generic pending-action path and will never produce tool transcript or tool execution rows
- the current tool-run matrix is intentionally split across existing tests rather than a single monolithic scenario
  - successful and failed execution coverage remains in `chat.test.ts`
  - approved and cancelled approval-gated coverage lives in `workspace.test.ts` and `auth-persistence.test.ts`

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t "records audit events when a pending action is cancelled"`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists cancelled pending actions across app restarts and records audit"`
- `npx vitest run apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/workspace.test.ts apps/gateway/src/routes/auth-persistence.test.ts -t "records completed and failed tool executions|records timed out tool executions as failed attempts|returns approval-required tool calls as pending actions without recording executions yet|records transcript and tool execution details when approval-required tools are approved|records audit events when a pending action is cancelled|persists runtime tool calls and tool result messages across app restarts|persists cancelled pending actions across app restarts and records audit|persists approval-required tool responses across app restarts"`
- `npm run build`

Observed results:

- `type-check` passed
- focused cancel route test passed
- focused cancel persistence test passed
- matrix Vitest run passed with `3` files, `7` tests passed, `59` skipped
- `build` passed

## P3-B-12 Tool onboarding and end-to-end test guide

Completed `P3-B-12` by writing a concrete runbook for adding a new tool safely and verifying it across shared contracts, runtime, persistence, replay, admin policy override, and restart coverage.

Implemented in this round:

- added:
  - `docs/guides/P3-B_TOOL_ONBOARDING_AND_E2E.md`
- documented:
  - required implementation order
  - minimum route/persistence/UI test matrix
  - recommended validation commands
  - current gotchas around:
    - approval-gated tool selection
    - structured tool failure taxonomy
    - legacy `metadata.failureReason` compatibility

Continuity notes:

- this guide should be updated whenever a new tool-stage failure code or approval mode is introduced
- the guide is intended to be the handoff entry point before adding any new built-in tool

## P3-C-01 Conversation presence/session model

Completed `P3-C-01` by adding the first presence/session boundary for multiple viewers on the same owner conversation surface.

Implemented in this round:

- extended shared contracts in:
  - `packages/shared/src/apps/contracts.ts`
  - added:
    - `WorkspaceConversationPresenceEntry`
    - `WorkspaceConversationPresence`
    - `WorkspaceConversationPresenceUpdateRequest`
    - `WorkspaceConversationPresenceResponse`
- added shared presence helpers in:
  - `apps/gateway/src/services/workspace-presence.ts`
  - the current model is:
    - process-local
    - ttl-based
    - session keyed
    - per-conversation
- added owner routes in:
  - `apps/gateway/src/routes/workspace.ts`
  - new endpoints:
    - `GET /workspace/conversations/:conversationId/presence`
    - `PUT /workspace/conversations/:conversationId/presence`
- wired both memory and persistent workspace services:
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
- added same-origin web client helpers:
  - `apps/web/src/lib/apps-client.ts`
  - `fetchWorkspaceConversationPresence(...)`
  - `updateWorkspaceConversationPresence(...)`

Continuity notes:

- current presence is intentionally ephemeral
  - it is process-local and ttl-pruned
  - it does not survive gateway restart
  - this is acceptable for collaboration presence, but not for durable comments or review state
- `sessionId` is client-provided
  - the server does not derive it from auth session token
  - future browser work should keep one stable client session id per tab/surface
- `P3-C-01` only covers owner conversation presence
  - shared transcript presence, collaborator chips, and multi-user UI are later tasks

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t "tracks conversation presence sessions for multiple viewers"`
- `npx vitest run apps/web/src/lib/apps-client.test.ts -t "conversation presence"`

Observed results:

- `type-check` passed
- presence route test passed
- client presence tests passed

## P3-C-02 Chat live refresh and presence heartbeat

Completed `P3-C-02` by wiring polling-based live refresh and presence heartbeat into the owner conversation detail surface.

Implemented in this round:

- added web presence helpers in:
  - `apps/web/src/lib/workspace-presence.ts`
  - helpers:
    - `readOrCreateWorkspacePresenceSessionId(...)`
    - `summarizeWorkspacePresence(...)`
- added helper coverage in:
  - `apps/web/src/lib/workspace-presence.test.ts`
- updated owner conversation detail in:
  - `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
  - the page now:
    - keeps one stable per-tab `sessionStorage` presence id
    - sends presence heartbeats through the existing owner route
    - polls for fresh conversation state while the tab is visible
    - surfaces viewer counts and last-sync time in the header

Continuity notes:

- the per-tab presence key is `workspace.presence.<conversationId>`
  - later shared-surface work should reuse the same idea instead of generating new ephemeral ids on every render
- the current polling interval is `10_000ms`
  - this is intentionally conservative to keep chat replay and collaboration refresh cheap while there is no push transport yet
- live refresh currently scopes to the owner conversation page
  - `P3-C-03+` should extend collaboration UI onto shared transcript surfaces instead of duplicating polling logic elsewhere
- the page only full-refreshes conversation state when the tab is visible and not in a local streaming/stop transition
  - hidden tabs still send heartbeat updates so presence stays accurate without extra transcript churn

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/web/src/lib/workspace-presence.test.ts apps/web/src/lib/apps-client.test.ts`
- `npm run build`

Observed results:

- type-check passed
- web presence helper and client tests passed
- production build passed

## P3-C-03 Shared conversation viewer identity chips

Completed `P3-C-03` by extending presence onto shared conversation surfaces and rendering collaborator identity chips on the read-only shared transcript page.

Implemented in this round:

- extended presence surface contracts in:
  - `packages/shared/src/apps/contracts.ts`
  - added `shared_conversation` to `WorkspaceConversationPresenceSurface`
- added shared presence routes in:
  - `apps/gateway/src/routes/workspace.ts`
  - new endpoints:
    - `GET /workspace/shares/:shareId/presence`
    - `PUT /workspace/shares/:shareId/presence`
- wired shared presence lookup/update in:
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - these routes reuse the same TTL-pruned per-conversation presence map as owner surfaces
- added shared presence client helpers in:
  - `apps/web/src/lib/apps-client.ts`
  - `fetchWorkspaceSharedConversationPresence(...)`
  - `updateWorkspaceSharedConversationPresence(...)`
- updated the shared transcript UI in:
  - `apps/web/src/app/(main)/chat/shared/[shareId]/page.tsx`
  - shared pages now:
    - keep a stable `sessionStorage` presence id keyed by `shared:<shareId>`
    - heartbeat with `surface = shared_conversation`
    - render collaborator identity chips with viewer state and surface labels
    - surface live-sync counts and last-sync timestamp in the header

Continuity notes:

- shared and owner presence intentionally share one underlying conversation presence set
  - chips can therefore show both `owner` and `shared` viewers when the same conversation is open from multiple surfaces
- the shared page reuses the same 10-second heartbeat cadence as the owner conversation page
  - there is still no push transport; future cursor/comment work should build on this instead of adding a second polling loop
- shared presence routes are authorization-scoped by share membership
  - a user who can open the shared transcript can also contribute a shared-surface presence heartbeat, but cannot write to the owner conversation route

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t "tracks shared conversation presence sessions for shared viewers|tracks conversation presence sessions for multiple viewers"`
- `npx vitest run apps/web/src/lib/apps-client.test.ts apps/web/src/lib/workspace-presence.test.ts`
- `npm run build`

Observed results:

- type-check passed
- shared presence route tests passed
- web client + helper tests passed
- production build passed

## P3-C-09 Collaboration browser coverage

Completed `P3-C-09` by stabilizing the multi-user collaboration browser flows on the default Chinese UI and adding shared transcript presence coverage for multiple viewers.

Implemented in this round:

- updated `tests/e2e/phase1-flows.spec.ts`
  - added locale-aware chat/shared transcript selectors
  - switched shared collaboration browser flows onto the stable `Tenant Control` app path
  - added a second browser-context test that verifies multiple shared viewers appear in the presence chips
- updated `tests/e2e/knowledge-flows.spec.ts`
  - scoped admin knowledge-source navigation to the admin nav rail
  - replaced the remaining English-only chat/admin selectors with locale-aware ones so retrieval browser coverage stays green under default Chinese

Continuity notes:

- isolated manual Playwright runs on `3118/4000` do not perform the quota top-up from `scripts/run-e2e.mjs`
  - if raw browser tests start failing with quota-blocked app launches, lift `workspace_quota_limits.monthly_limit` for `dev-tenant` before rerunning
- `apply_patch` can still truncate large files on timeout
  - this round recovered `tests/e2e/phase1-flows.spec.ts` with `git show HEAD:path > path` before reapplying the intended edits
- shared collaboration coverage should keep using a stable Research Lab launchable app
  - `Tenant Control` is now the safest choice for the shared transcript tests

Validation performed in this round:

- `npm run type-check`
- `LD_LIBRARY_PATH=/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu PLAYWRIGHT_BASE_URL=http://127.0.0.1:3118 npx playwright test tests/e2e/phase1-flows.spec.ts -g 'conversation shares allow another group member to open a read-only shared transcript|shared transcripts show presence chips for multiple viewers'`
- `LD_LIBRARY_PATH=/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu PLAYWRIGHT_BASE_URL=http://127.0.0.1:3118 npx playwright test tests/e2e/knowledge-flows.spec.ts`

Observed results:

- type-check passed
- shared collaboration browser coverage passed (`2` tests)
- knowledge retrieval browser coverage passed (`1` test)

## P3-C-04 Workspace comment threads

Completed `P3-C-04` by adding a unified conversation-scoped comment boundary that can attach notes to messages, runs, and artifacts.

Implemented in this round:

- extended the shared workspace contracts in:
  - `packages/shared/src/apps/contracts.ts`
  - added `WorkspaceComment`, comment create request/response types, and comment arrays on message/run/artifact payloads
- added persistent storage in:
  - `packages/db/src/schema/workspace.ts`
  - `packages/db/migrations/0016_p3_c4_workspace_comments.sql`
  - comments now live in a dedicated `workspace_comments` table
- updated workspace services and routes in:
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - `apps/gateway/src/routes/workspace.ts`
  - new create route:
    - `POST /workspace/conversations/:conversationId/comments`
- added owner-facing UI in:
  - `apps/web/src/components/workspace-comments.tsx`
  - `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
  - `apps/web/src/app/(main)/chat/artifacts/[artifactId]/page.tsx`
  - message bubbles, run replay, and artifact preview now render the persisted comment thread and allow owner-side note creation

Continuity notes:

- comment creation is intentionally conversation-scoped
  - the target entity is selected with `targetType + targetId`
  - this keeps route sprawl down while comments are still owner-scoped
- comment data is stored separately from transcript/run payload blobs
  - this prevents later run/transcript rewrites from dropping existing review notes
- current write support is owner-surface only
  - shared surfaces can reuse the same read payloads later, but shared write permissions remain a separate collaboration policy task

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t 'creates workspace comment threads for messages, runs and artifacts'`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t 'persists workspace comments for messages, runs and artifacts across app restarts'`
- `npm run build`

Observed results:

- type-check passed
- targeted memory route coverage passed
- targeted persistence restart coverage passed
- production build passed

## P3-C-08 Comment audit coverage

Completed `P3-C-08` by wiring the new workspace comment write path into audit and proving the audit survives restart.

Implemented in this round:

- extended audit contracts in:
  - `packages/shared/src/auth/contracts.ts`
  - added:
    - `workspace.comment.created`
    - `conversation_comment`
- updated the comment create route in:
  - `apps/gateway/src/routes/workspace.ts`
  - comment creation now records one audit event per successful note
- tightened test coverage in:
  - `apps/gateway/src/routes/workspace.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
  - both memory and persistent coverage now assert the emitted audit payload for message/run/artifact comments

Continuity notes:

- the audit payload for comment creation now includes:
  - `conversationId`
  - `targetType`
  - `targetId`
  - `threadLength`
- the route currently emits only `workspace.comment.created`
  - if shared write support or edit/delete semantics are added later, extend this action family instead of creating unrelated audit names

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t 'creates workspace comment threads for messages, runs and artifacts'`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t 'persists workspace comments for messages, runs and artifacts across app restarts'`

Observed results:

- type-check passed
- targeted memory route coverage passed with audit assertions
- targeted persistence restart coverage passed with audit assertions

## Shared transcript comment rendering follow-up

Added read-only comment rendering onto the shared transcript page in:

- `apps/web/src/app/(main)/chat/shared/[shareId]/page.tsx`

This reuses the same `WorkspaceCommentThread` component introduced for owner surfaces, but keeps the composer hidden on shared views.

Continuity notes:

- shared artifact preview already inherited read-only comment rendering from the owner artifact preview page because it uses the same route/component with `shareId`
- do not run `npm run type-check` and `npm run build` for `@agentifui/web` in parallel on this host
  - both commands touch `.next/types`
  - running them concurrently can trigger a false-negative missing `routes.ts` error

Validation performed in this follow-up:

- `npm run build`
- `npm run type-check`

Observed results:

- build passed
- type-check passed when rerun serially

## P3-C-05 Mention and review inbox primitives

Completed `P3-C-05` by adding `@email` comment mentions, persisted collaboration notifications, and a first review inbox surface on `/apps`.

Implemented in this round:

- shared contracts:
  - `packages/shared/src/apps/contracts.ts`
  - added:
    - `WorkspaceCommentMention`
    - `WorkspaceNotification`
    - `WorkspaceNotificationsResponse`
    - `WorkspaceNotificationReadResponse`
- shared workspace runtime helpers:
  - `apps/gateway/src/services/workspace-comments.ts`
  - added:
    - comment normalization
    - `@email` mention extraction
    - comment preview generation
- gateway route/service changes:
  - `apps/gateway/src/routes/workspace.ts`
  - `apps/gateway/src/services/workspace-service.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - added:
    - mention resolution against same-tenant users
    - access gating so only owner or active-share collaborators get notified
    - `GET /workspace/notifications`
    - `PUT /workspace/notifications/:notificationId/read`
- persistence:
  - `packages/db/src/schema/workspace.ts`
  - `packages/db/migrations/0017_p3_c5_workspace_notifications.sql`
  - `workspace_comments` now stores `mentions`
  - new `workspace_notifications` table stores unread/read mention notifications
- web:
  - `apps/web/src/lib/apps-client.ts`
  - `apps/web/src/app/(main)/apps/page.tsx`
  - `apps/web/src/components/workspace-comments.tsx`
  - `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
  - `apps/web/src/app/(main)/chat/artifacts/[artifactId]/page.tsx`
  - `/apps` now shows a review inbox and comment composers explain the `@email` mention format
- operational support:
  - `scripts/lib/backup-helpers.mjs`
  - backups now include:
    - `workspace_comments`
    - `workspace_notifications`

Continuity notes:

- mention syntax is intentionally email-first for now
  - use `@user@example.com`
  - free-form `@Display Name` lookup is not implemented yet
- mention notifications only fan out to users who can already access the conversation
  - owner access is allowed
  - shared reviewers must belong to an active shared group
  - unresolved or inaccessible mentions stay as plain text in the comment body and do not generate notifications
- the first inbox surface lives on `/apps`
  - there is not yet a global nav badge or auto-mark-read-on-open behavior
  - read state is explicit through `PUT /workspace/notifications/:notificationId/read`
- shared transcript and shared artifact views stay read-only
  - they can render comments and mention chips
  - they still do not expose comment creation
- keep running `npm run type-check` and `npm run build` serially on this host
  - both touch `.next/types`
  - concurrent runs can still manufacture a false missing `routes.ts`

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts -t 'creates workspace comment threads for messages, runs and artifacts'`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t 'persists workspace comments for messages, runs and artifacts across app restarts'`
- `npx vitest run apps/web/src/lib/apps-client.test.ts`
- `npm run build`

Observed results:

- type-check passed
- targeted memory route coverage passed with mention + notification assertions
- targeted persistence restart coverage passed with mention + notification assertions
- apps client tests passed (`23` tests)
- production build passed
