# 2026-03-16 P3-A-09

Completed `P3-A-09` by adding a dedicated persistence test for knowledge ingestion transitions and retrieval continuity across app restarts.

Coverage added:

- `apps/gateway/src/routes/knowledge-persistence.test.ts`
  - create group-scoped knowledge source
  - drive `queued -> processing -> failed -> succeeded`
  - persist chunk rows in `knowledge_source_chunks`
  - restart the app
  - verify `/admin/sources` still shows the succeeded source
  - launch `Policy Watch`
  - verify the persisted run keeps retrieval matches for the active group only

Validation:

- `npm run type-check`
- `npx vitest run apps/gateway/src/routes/knowledge-persistence.test.ts`

Continuity notes:

- this dedicated file was added instead of extending the very large `auth-persistence.test.ts`, which keeps targeted retries simpler on this host
- next active item is `P3-A-10`
