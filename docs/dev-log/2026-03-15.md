# 2026-03-15

## P2-D3 Prompt Injection And Safety Signals

Completed `P2-D3` by threading safety metadata through the shared contracts, gateway placeholder runtime, workspace persistence layer, replay surfaces, shared transcript, and audit pipeline.

Implemented in this round:

- shared contracts now include:
  - `WorkspaceSafetySignal`
  - `WorkspaceConversationMessage.safetySignals`
  - `WorkspaceRun.safetySignals`
  - `chat.completion` / `chat.completion.chunk` `safety_signals`
  - audit action `workspace.run.safety_flagged`
- gateway runtime now includes:
  - heuristic safety detection for:
    - `prompt_injection`
    - `data_exfiltration`
    - `policy_violation`
  - runtime hook support via `body.inputs.safetySignals` / `body.inputs.safety_signals`
  - safety metadata persisted in both:
    - `outputs.safetySignals`
    - `outputs.assistant.safetySignals`
  - flagged-run audit writes with `warning` vs `critical` level selection
- persistence/runtime services now include:
  - rehydration of safety metadata from `conversation_inputs`
  - run rehydration from both top-level and nested assistant safety payloads
  - compatibility fallback for rows that do not yet contain any safety metadata
- web surfaces now include:
  - a conversation-level `Safety review` banner on `/chat/[conversationId]`
  - per-message safety panels in the live transcript
  - replayable run safety panels and meta cards in `Run history`
  - read-only shared transcript safety panels that intentionally hide reviewer-only detail

Useful continuity notes for future sessions:

- on this host, large `apply_patch` operations against very large files are still risky
  - `apps/gateway/src/routes/chat.ts`
  - `apps/gateway/src/services/persistent-workspace-service.ts`
  - if a patch times out and truncates a file, restore with:
    - `git show HEAD:path/to/file > path/to/file`
  - for large behavioral additions, prefer new helper modules and small follow-up patches
- safety masking rule for public/shared surfaces:
  - owner workspace shows `summary + detail + recommended action`
  - shared transcript uses `publicView`
  - shared transcript only shows `summary + severity/category`, never the detailed reviewer note
- admin audit filtering did not need a new route change
  - `/admin/audit` already accepts a free-text `action`
  - after this round, filter with `workspace.run.safety_flagged`
- targeted browser verification on this host was run with an isolated stack:
  - `node scripts/prepare-playwright-runtime.mjs`
  - `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run db:reset`
  - gateway on `4216`
  - web on `3117`
  - `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3117 npx playwright test tests/e2e/phase1-flows.spec.ts --grep "flagged prompts render safety banners and replay panels"`
- full `npm run test:persistence` is still flaky on this shared host
  - this round it advanced to `1/21` and then stalled again without surfacing a deterministic feature failure
  - targeted persistence for the new safety path passed and was used to close `P2-D3`

Validation performed in this round:

- `npm run type-check`
- `npm run test:unit`
  - passed with `24` files / `149` tests
- targeted Vitest:
  - `npx vitest run apps/gateway/src/services/workspace-safety.test.ts`
  - `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists safety signals and audit events across app restarts"`
- targeted Playwright:
  - `npx playwright test tests/e2e/phase1-flows.spec.ts --grep "flagged prompts render safety banners and replay panels"`
- attempted full persistence:
  - `npm run test:persistence`
  - host-level stall reproduced after the first test started

Next active item:

- `P2-D4` app runtime abstraction

## P2-D4 App Runtime Abstraction

Completed `P2-D4` by extracting the placeholder protocol path into a formal runtime adapter service, wiring runtime metadata into run persistence, surfacing runtime health from the gateway, and adding a browser smoke for a non-default adapter path.

Implemented in this round:

- gateway runtime boundary now includes:
  - `apps/gateway/src/services/workspace-runtime.ts`
  - adapter health snapshots and capability flags
  - default `placeholder` adapter
  - `placeholder_structured` adapter used by `Runbook Mentor`
- gateway route/runtime wiring now includes:
  - `buildApp()` runtime service injection
  - `/health` runtime readiness payload
  - `/v1/chat/completions` runtime invocation before persistence closeout
  - structured run failure persistence for runtime outages
  - `ChatCompletionResponse.metadata.runtime_id`
- workspace persistence now includes:
  - `WorkspaceRun.runtime`
  - rehydration from `outputs.runtime` in both in-memory and persistent services
  - runtime metadata visible through `/workspace/runs/:runId`
- web replay surface now includes:
  - runtime meta cards in `/chat/[conversationId]` run history
  - runtime capability/status text in the replay panel
- verification/docs now include:
  - route and service tests for runtime selection + degraded adapter failure
  - persistence assertion that runtime metadata survives restart
  - browser smoke for `Runbook Mentor`
  - `docs/guides/P2-D4_RUNTIME_ADAPTER_GUIDE.md`

Useful continuity notes for future sessions:

- do not trust the long-running default `3111` web process on this host without checking its target gateway first
  - the stale process inspected in this round had:
    - `PORT=3111`
    - `GATEWAY_INTERNAL_URL=http://127.0.0.1:4111`
  - because `4111` was not listening, Playwright hit a client-side exception on `/apps`
  - check with:
    - `tr '\0' '\n' < /proc/<next-pid>/environ | rg 'PORT|GATEWAY_INTERNAL_URL'`
- when browser verification needs a clean stack, prefer an isolated production pair instead of reusing stale dev servers
  - build first:
    - `npm run build`
  - start gateway:
    - `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui GATEWAY_PORT=4118 BETTER_AUTH_SECRET=agentifui-e2e-super-secret-1234567890 GATEWAY_SSO_DOMAINS='iflabx.com=iflabx-sso' npm run start --workspace @agentifui/gateway`
  - start web:
    - `PORT=3118 GATEWAY_INTERNAL_URL=http://127.0.0.1:4118 npm run start --workspace @agentifui/web`
  - run browser smoke:
    - `LD_LIBRARY_PATH=/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu PLAYWRIGHT_BASE_URL=http://127.0.0.1:3118 npx playwright test tests/e2e/phase1-flows.spec.ts --grep "runbook mentor browser flow uses the structured runtime path"`
- `Runbook Mentor` is now the canonical non-default runtime smoke target
  - it routes to `placeholder_structured`
  - the browser assertion should scope duplicate runtime text with `.first()` because the label appears in both the meta card and the replay bubble
- `WorkspaceRun.runtime` is replay-only metadata
  - keep it JSON-safe
  - never attach provider clients, tokens, raw headers, or transport handles

Validation performed in this round:

- `npm run type-check`
- `npm run build`
- `npm run test:unit`
  - passed with `25` files / `153` tests
- targeted Vitest:
  - `npx vitest run apps/gateway/src/services/workspace-runtime.test.ts apps/gateway/src/app.test.ts apps/gateway/src/routes/chat.test.ts`
  - `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists chat completion run updates and keeps the trace id stable"`
- targeted Playwright:
  - `LD_LIBRARY_PATH=/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu PLAYWRIGHT_BASE_URL=http://127.0.0.1:3118 npx playwright test tests/e2e/phase1-flows.spec.ts --grep "runbook mentor browser flow uses the structured runtime path"`

Next active item:

- `P2-D5` degraded fallback

## Long-Range Checkbox Board Expansion

Expanded the long-range execution board again so future sessions have a much deeper backlog with checkbox-scoped work items instead of coarse milestone labels.

Recorded in this round:

- `docs/plans/PHASE2_EXECUTION_PLAN.md` now extends beyond `P3-H` into:
  - `P4-A` multi-provider runtime and model routing
  - `P4-B` external connectors and sync
  - `P4-C` agent planning and branching
  - `P4-D` workflow builder and authoring
  - `P4-E` billing and commercial controls
  - `P4-F` enterprise security and compliance policy packs
  - `P4-G` observability, incident response, and SLO management
  - `P4-H` automated QA and release certification
  - `P4-I` documentation and AI-session continuity
- every newly added task uses an explicit checkbox so a future session can mark progress in-place
- the active work item remains `P2-D4`

Useful continuity notes for future sessions:

- this longer backlog is meant to be the canonical planning surface
  - do not create a separate ad hoc plan unless the structure itself needs revision
- when a delivery item completes, update three places together:
  - the status table near the top of `PHASE2_EXECUTION_PLAN.md`
  - the checkbox row in section `7`
  - the latest dev log entry with implementation and validation notes

Validation performed in this round:

- no tests were run
- this was a docs-only planning expansion
