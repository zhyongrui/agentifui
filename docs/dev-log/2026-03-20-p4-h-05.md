# 2026-03-20 P4-H-05

## Goal
- close `P4-H-05` by adding host-capability checks so browser entry scripts can skip safely when runtime dependencies are unavailable

## Inputs Reviewed
- `scripts/prepare-playwright-runtime.mjs`
- `scripts/run-e2e.mjs`
- `scripts/public-browser-smoke.mjs`
- `docs/guides/P4-H_QA_MATRIX.md`

## Implementation
- added `getPlaywrightHostCapability()` to the Playwright runtime prep script
- browser entry scripts now:
  - detect unsupported or dependency-missing hosts before doing expensive work
  - print a clear skip reason
  - keep hard-fail behavior available through `PLAYWRIGHT_STRICT_HOST_CHECK=1`
- updated the QA matrix guide to document the new strict-vs-skip behavior
- marked `P4-H-05` complete in the execution plan

## Validation
- `PLAYWRIGHT_FORCE_HOST_UNAVAILABLE=1 node scripts/public-browser-smoke.mjs`
- `PLAYWRIGHT_FORCE_HOST_UNAVAILABLE=1 node scripts/run-e2e.mjs`

Observed results:

- public browser smoke skipped cleanly with an explicit reason
- e2e wrapper skipped cleanly with an explicit reason

## Notes
- this round only changes host-capability gating; it does not alter the existing browser assertions or suite contents
