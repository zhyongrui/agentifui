# 2026-03-16 P3-A-08

Completed `P3-A-08` by turning the retrieval visibility rule into an explicit access-control helper and adding unit coverage around group-scoped indexing.

Key wiring:

- `apps/gateway/src/services/knowledge-retrieval.ts`
  - added `isKnowledgeSourceAccessibleToGroup(...)`
- `apps/gateway/src/services/knowledge-service.ts`
  - in-memory retrieval now reuses the helper instead of inline scope checks
- `apps/gateway/src/services/knowledge-service.test.ts`
  - verifies retrieval includes:
    - tenant-scoped matches
    - active-group matches
  - verifies retrieval excludes:
    - mismatched group-scoped sources

Validation:

- `npm run type-check`
- `npx vitest run apps/gateway/src/services/knowledge-service.test.ts apps/gateway/src/services/knowledge-retrieval.test.ts apps/gateway/src/routes/chat.test.ts`
- `npm run build`

Continuity notes:

- the persisted retrieval query in `persistent-knowledge-service.ts` already used the same tenant-or-active-group rule; this round made the rule explicit and testable on the in-memory service path too
- next active item is `P3-A-09`
