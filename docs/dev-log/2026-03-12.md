# Development Log - 2026-03-12

## Goal

Execute `R4-A` in the Phase 1 rolling plan by replacing the default in-memory auth runtime with a PostgreSQL-backed implementation while keeping the existing `/auth/*` contracts stable.

## Inputs Reviewed

This round continued against:

- `docs/plans/PHASE1_DEVELOPMENT_PLAN.md`
- `/home/bistu/zyr/pros/agentifui-docs/frd/AFUI-FRD-S1-1.md`
- `/home/bistu/zyr/pros/agentifui-docs/roadmap/PHASE1_ACCEPTANCE.md`

## R4-A Persistent Auth Foundation

Completed the first persistent-auth replacement pass for `S1-1`.

Implemented in `packages/db`:

- added runtime connection helpers in `packages/db/src/runtime.ts`
- added SQL migration directory under `packages/db/migrations/`
- added root scripts:
  - `npm run db:migrate`
  - `npm run db:reset`
- extended schema coverage for:
  - persisted failed-login counters and lockout timestamps
  - auth sessions
  - auth challenges for MFA setup / MFA login tickets

Adjusted the schema model to use string ids instead of UUID-only columns so the runtime can safely persist current ids such as:

- tenant ids like `dev-tenant`
- group ids like `grp_product`

## Gateway Auth Persistence

Added DB-backed service implementations in `apps/gateway`:

- `persistent-auth-service.ts`
- `persistent-audit-service.ts`

The default `buildApp` path now:

- loads `DATABASE_URL`
- runs migrations on startup
- ensures the default tenant exists
- uses persistent auth/audit services when database config is available
- falls back to the existing in-memory service only when no database is configured or tests explicitly inject doubles

Persisted state now includes:

- users
- password identities
- SSO identities
- invitations
- MFA factors
- MFA setup challenges
- MFA login tickets
- audit events
- sessions

Behavioral changes completed in this round:

- logout now revokes the session instead of only returning a success response
- sessions survive app restarts
- MFA enrollment survives app restarts
- invitation consumption survives app restarts
- audit events survive app restarts

## Test Coverage Added

Added `apps/gateway/src/routes/auth-persistence.test.ts`.

This integration suite verifies:

- session persistence across app restart
- logout invalidates the persisted session
- MFA enrollment remains effective after restart
- invitation tokens cannot be reused after restart

Updated existing audit-route expectations so they keep passing after logout starts revoking sessions.

## Environment and Runtime Notes

Local PostgreSQL was available in this environment, but the old example connection string used a missing `postgres` role.

To stabilize local development, the repository now uses:

- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui`

Also created a matching local role/database in the current machine so app runtime, tests and reset scripts use one consistent dev path.

## Validation

Validated this round with:

- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run type-check`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm test`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run build`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run db:reset`

Functional HTTP smoke was executed against the running gateway on `http://127.0.0.1:4000`:

- register -> `201`
- login -> `200`
- mfa setup -> `200`
- mfa enable -> `200`
- login after MFA enabled -> `401 AUTH_MFA_REQUIRED`
- mfa verify -> `200`
- audit query -> `200`
- logout -> `200`
- workspace access after logout -> `401 WORKSPACE_UNAUTHORIZED`

Public tunnel checks also passed:

- `GET https://b9bd-121-194-33-60.ngrok-free.app/login` -> `200`
- `GET https://2fdc-121-194-33-60.ngrok-free.app/health` -> `200`

## Next Step

Activate `R4-B`:

- integrate `better-auth` on top of the persisted auth boundary
- keep current route contracts stable
- finish the remaining `R4` work before moving to `R5` RBAC persistence

---

## R4-B Better-Auth Integration

Completed the second half of `R4` by introducing better-auth as the internal auth/session runtime while preserving the current gateway contracts.

Implemented in this round:

- added better-auth runtime wiring in `apps/gateway/src/services/better-auth-core.ts`
- added shared password hashing helper for gateway persistence and better-auth
- extended gateway env parsing with:
  - `BETTER_AUTH_SECRET`
  - `BETTER_AUTH_URL`
- bootstrapped better-auth during app startup when `DATABASE_URL` is present
- mapped better-auth user storage onto the existing `users` table
- added better-auth tables and schema exports:
  - `better_auth_accounts`
  - `better_auth_sessions`
  - `better_auth_verifications`
- updated persistent auth service to:
  - verify credential passwords via better-auth
  - create and revoke sessions via better-auth
  - keep custom business rules for lockout, pending, MFA, invitation and SSO flows

Behavioral result:

- current `/auth/register`, `/auth/login`, `/auth/logout`, `/auth/mfa/*`, `/auth/sso/*` and invitation routes remain stable
- session persistence now uses better-auth session tables as the primary store
- password-based accounts are mirrored into better-auth credential accounts for transition safety

## R5-A Workspace Authz Persistence

Completed the first persistence slice for `S1-2 / S1-3` workspace authorization.

Implemented in this round:

- added `workspace_apps` and `workspace_group_app_grants`
- added DB schema exports and migration `0003_r5_a_workspace_authz.sql`
- added `persistent-workspace-service.ts`
- seeded default workspace groups, apps and group->app grants during app startup
- changed `/workspace/apps` to use the DB-backed service whenever the gateway runs with PostgreSQL
- persisted default user memberships lazily on first workspace access
- preserved stable workspace app ordering with an explicit persisted `sort_order`

Behavioral result:

- normal users still see:
  - `grp_product`
  - `grp_research`
  - `app_market_brief`
  - `app_service_copilot`
  - `app_release_radar`
  - `app_policy_watch`
  - `app_runbook_mentor`
- security/audit users now resolve to:
  - `grp_security`
  - `app_audit_lens`
- the above result now comes from PostgreSQL instead of in-memory grants

## Additional Test Coverage

Extended automated coverage to lock the new behavior:

- added persistent workspace integration coverage that verifies:
  - default `group_members` are created on first workspace access
  - normal users and security users receive different DB-backed app catalogs
  - workspace app order remains stable after moving to the database
- added env coverage for production `BETTER_AUTH_SECRET` enforcement

## Validation

Validated after finishing `R4-B` and `R5-A` with:

- `npm run type-check`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run db:reset`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm test`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run build`

Functional HTTP smoke against the existing local runtime:

- `GET http://127.0.0.1:4000/health` -> `200`
- `GET http://127.0.0.1:3000/login` -> `200`
- gateway register/login/workspace/logout path -> success
- workspace after logout -> `401 WORKSPACE_UNAUTHORIZED`
- security user workspace -> only `grp_security` / `app_audit_lens`

## Next Step

Activate `R5-B`:

- introduce DB-backed RBAC rule tables for role, allow/deny and user-direct grants
- keep the current workspace contract stable while moving authorization decisions behind a real rule engine
- prepare the data boundary needed by later admin authorization management

---

## R5-B RBAC Rule Evaluation

Completed the first real RBAC rule-evaluation pass for `S1-2`.

Implemented in this round:

- added DB schema and migration for:
  - `rbac_roles`
  - `rbac_user_roles`
  - `workspace_app_access_grants`
- introduced role seeds:
  - `root_admin`
  - `tenant_admin`
  - `user`
- changed the persistent workspace service to evaluate app visibility from DB grants instead of only group fixtures
- added the first role-gated admin workspace app:
  - `app_tenant_control`
- added default role assignment heuristics for dev/test:
  - `admin*` / `*owner*` emails receive `tenant_admin` + `user`
  - other users receive `user`

Authorization precedence now enforced in the workspace catalog:

1. explicit `deny`
2. direct `user allow`
3. `group allow` or `role allow`
4. default deny

Expired user grants are now ignored automatically.

## Production Runtime Packaging Fix

While validating this round through real processes instead of `app.inject`, the gateway production start path failed.

Root cause:

- workspace package exports still pointed to `src/*.ts`
- compiled shared-package output also contained extensionless relative exports that Node ESM would not resolve

Fix applied:

- changed `@agentifui/db` and `@agentifui/shared` package exports to serve `dist/*.js` for runtime
- kept source `.ts` entries for type resolution
- normalized shared-package re-export paths to `.js`

Result:

- `npm run start --workspace @agentifui/gateway` now starts successfully from built output

## Validation

Validated the final state of this round with:

- `npm run type-check`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run db:reset`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm test`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run build`

Persistent test coverage now verifies:

- role-based app visibility
- direct user allow
- explicit deny precedence
- expired user grants are ignored
- startup/seed behavior for default memberships and roles

Functional HTTP smoke was executed against isolated processes:

- gateway: `http://127.0.0.1:4100`
- web: `http://127.0.0.1:3101`

Smoke result:

- `GET /health` -> `200`
- `GET /login` -> `200`
- admin user workspace includes `app_tenant_control`
- developer user after direct allow + deny sees:
  - `app_audit_lens` added
  - `app_release_radar` removed
- developer workspace via same-origin `/api/gateway/workspace/apps` -> `200`
- logout -> `200`
- workspace after logout -> `401 WORKSPACE_UNAUTHORIZED`

## Next Step

Activate `R6-A`:

- persist recent apps and favorites
- define the first real workspace launch contract
- prepare the handoff from `/apps` into real app/session creation

---

## Public Preview Hotfix

Follow-up issue discovered during browser validation:

- the public web preview was reachable
- the gateway was reachable
- but browser-side auth requests still attempted to reach the gateway directly instead of going through a same-origin web endpoint

This caused the public login page to surface:

- `Unable to reach the auth gateway. Check the gateway server and try again.`

Hotfix implemented:

- added `apps/web/src/app/api/gateway/[...path]/route.ts`
- changed browser auth/workspace clients to call `/api/gateway/*`
- added `GATEWAY_INTERNAL_URL` support for the web server-side proxy target
- added web client tests for the same-origin proxy path
- changed persistent DB integration tests to `describe.sequential` and increased timeout so the full test suite can run without shared-database contention

Hotfix validation:

- `POST http://127.0.0.1:3100/api/gateway/auth/sso/discovery` -> `200`
- `POST https://b9bd-121-194-33-60.ngrok-free.app/api/gateway/auth/sso/discovery` -> `200`
- `POST https://b9bd-121-194-33-60.ngrok-free.app/api/gateway/auth/login` with `browser-demo@iflabx.com / Secure123` -> `200`

---

## MFA Setup UX Hotfix

During live browser testing, the security page exposed only the raw manual key and OTPAuth URI after starting MFA setup.

This was functional but not sufficient for normal phone-authenticator onboarding because:

- there was no QR code to scan
- the manual key block was easy to miss

Hotfix implemented:

- added client-side QR code rendering on `/settings/security`
- added clearer MFA setup instructions
- added a manual-key copy action

Validation:

- `npm run type-check --workspace @agentifui/web`
- `npm run build --workspace @agentifui/web`
- `npm test`

---

## Browser E2E Baseline And Contract Hardening

This round moved browser validation from ad-hoc manual checks into a repeatable Playwright regression path and fixed the real issues exposed by that path.

### What was added

- root-level Playwright setup:
  - `playwright.config.ts`
  - `tests/e2e/phase1-flows.spec.ts`
  - `npm run test:e2e`
- an isolated E2E launcher in `scripts/run-e2e.mjs`
  - builds the repo
  - resets PostgreSQL state
  - starts Gateway on `4111`
  - starts Web on `3111`
  - injects `GATEWAY_SSO_DOMAINS=iflabx.com=iflabx-sso`
- Linux runtime preparation in `scripts/prepare-playwright-runtime.mjs`
  - downloads the missing Playwright browser `.deb` packages in user space
  - extracts the required shared libraries into `.cache/playwright-runtime-libs`
  - avoids requiring `sudo` on this machine
- browser-launch hardening:
  - disabled proxy inheritance for Playwright browser runs
  - added `--no-proxy-server` launch arg

### Product issues found and fixed by the browser run

1. `/apps` crashed after login because DB-backed `workspace_apps.tags` were returned as JSON strings instead of `string[]`.
   - fixed in `apps/gateway/src/services/persistent-workspace-service.ts`
   - added defensive normalization in `apps/web/src/lib/apps-client.ts`
   - extended tests so the workspace contract now locks `tags` as arrays

2. login could race with background SSO discovery when users immediately typed a password and submitted.
   - fixed in `apps/web/src/app/(auth)/login/page.tsx`
   - once a password is present, SSO discovery is cancelled/skipped for that input state

3. browser E2E assertions were aligned to the current UI contract:
   - generic password-policy error text on `/register`
   - stable SSO and profile/security selectors
   - current admin placeholder headings (`Users`, `Groups`, `Apps`, `Audit`)

### Validation

Validated this round with:

- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run type-check`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm test`
- `npm run test:e2e`

Browser regression result:

- `6 passed`
- covered flows:
  - register -> login -> workspace controls
  - SSO pending -> profile access boundary
  - invitation acceptance
  - MFA enable -> MFA login verify -> MFA disable
  - security/admin workspace catalog differences
  - admin placeholder pages

### Next Step

Keep `R6-A` active:

- persist recent apps and favorites
- define the first real workspace launch contract
- prepare the handoff from `/apps` into real app/session creation

## Workspace Navigation Hotfix

Manual preview exposed a UX gap: the `MFA` page existed at `/settings/security`, but there was no obvious in-product navigation path from the workspace or the admin placeholder pages, so testers had to type the route manually.

### Changes

- added a shared main-section nav in `apps/web/src/components/main-section-nav.tsx`
- exposed visible links for:
  - `Apps workspace`
  - `Profile`
  - `Security / MFA`
  - `Admin preview` when the workspace includes the tenant-control app
- mounted the nav in:
  - `apps/web/src/app/(main)/apps/page.tsx`
  - `apps/web/src/app/(main)/settings/profile/page.tsx`
  - `apps/web/src/app/(main)/settings/security/page.tsx`
  - `apps/web/src/app/admin/layout.tsx`
- added an explicit workspace badge reminding testers that the security path now lives behind `Security / MFA`

### Validation

- extended the Playwright workspace flow to click `Security / MFA` from `/apps`, assert `/settings/security`, then navigate back via `Apps workspace`

### Note

- the `admin/*` pages are still placeholders for `S3-1`; current manual testing value there is route reachability, not admin CRUD behavior

---

## R6-A Workspace Preferences And Launch Handoff

Completed the first half of `R6` by moving workspace state off browser-local storage and defining the first persisted launch handshake contract.

Implemented in this round:

- added workspace persistence tables and migration:
  - `workspace_user_preferences`
  - `workspace_app_launches`
- extended shared workspace contracts with:
  - persisted `favoriteAppIds` / `recentAppIds` on catalog responses
  - `WorkspacePreferences*`
  - `WorkspaceAppLaunch*`
  - new workspace error codes for invalid payload / not found / launch blocked
- extended gateway workspace services to:
  - read and sanitize persisted favorites / recents / default active group
  - persist workspace preferences in both in-memory and PostgreSQL runtimes
  - create `handoff_ready` launch rows
  - update recents on successful launch
- added new gateway routes:
  - `GET /workspace/preferences`
  - `PUT /workspace/preferences`
  - `POST /workspace/apps/launch`
- updated the web same-origin proxy to support `PUT`
- changed the web `/apps` page to:
  - hydrate from gateway-backed workspace preferences
  - persist favorites / recents / default group through the new preferences endpoint
  - call the real launch endpoint instead of only synthesizing client-side readiness messages

Behavioral result:

- workspace state is now account-scoped and survives restart
- launching an app now creates a persisted handoff record with:
  - launch id
  - launch url
  - attributed group
  - launch timestamp
- the current `launchUrl` still points back to `/apps` with launch query params; `R6-B` will move this into a real app/session surface

## Validation

Validated this round with:

- `npm run type-check`
- `npm test`
- targeted persistence verification against local PostgreSQL:
  - `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts`

Key automated coverage added:

- gateway in-memory route coverage for:
  - workspace preferences read/write
  - invalid workspace preference payloads
  - successful and blocked workspace launches
- persistent workspace coverage for:
  - preferences surviving app restart
  - launch handoffs written to PostgreSQL
  - recents updated by successful launch
- web client coverage for:
  - `PUT /workspace/preferences`
  - `POST /workspace/apps/launch`

## Next Step

Activate `R6-B`:

- turn `workspace_app_launches` into real app/session creation
- move `launchUrl` from `/apps?...` handoff placeholders to an actual app surface
- prepare the key boundary that `S2-2 / S2-3` will need for conversation and run tracking

---

## R6-B Workspace Conversation Surface

Completed the second half of `R6` by turning workspace launch handoffs into real conversation/run creation and routing users into a minimal app surface.

Implemented in this round:

- extended DB schema with:
  - `conversations`
  - `runs`
  - `workspace_app_launches.conversation_id`
  - `workspace_app_launches.run_id`
  - `workspace_app_launches.trace_id`
  - `workspace_app_launch_status = conversation_ready`
- extended shared workspace contracts with:
  - `WorkspaceConversation`
  - `WorkspaceConversationResponse`
  - launch response fields:
    - `conversationId`
    - `runId`
    - `traceId`
- changed workspace launch behavior in gateway services:
  - successful launch now creates a real conversation row
  - creates the initial pending run row with a trace id
  - updates the launch row to `conversation_ready`
  - returns `/chat/{conversationId}` as the launch URL
- added `GET /workspace/conversations/:conversationId`
- added a new web page:
  - `/chat/[conversationId]`
  - fetches the workspace conversation through the same-origin gateway proxy
  - shows the app, group, run and trace context as the first real post-launch surface
- changed `/apps` so a successful launch now navigates directly into the new chat shell

Behavioral result:

- launching an app no longer stops at a readiness toast on `/apps`
- the user enters a real route immediately after launch
- the route is backed by persisted conversation/run rows, not only client state
- `R6` now leaves concrete ids in place for the next `S2-1 / S2-2 / S2-3` work:
  - conversation id
  - run id
  - trace id

## Validation

Validated this round with:

- `npm run type-check`
- `npm test`
- `npm run test:e2e`

Targeted functional smoke against a running local gateway:

- `POST /auth/register` -> `201`
- `POST /auth/login` -> `200`
- `POST /workspace/apps/launch` -> `200 conversation_ready`
- `GET /workspace/conversations/:conversationId` -> `200`

Browser regression result:

- `6 passed`
- updated workspace flow now verifies:
  - workspace launch redirects into `/chat/[conversationId]`
  - the new chat shell shows bootstrap context
  - returning to `/apps` still preserves recent/favorite behavior

## Next Step

Activate `R7`:

- wire `/v1/chat/completions` onto the new conversation/run boundary
- reuse the persisted trace id through the gateway protocol
- start replacing the current placeholder chat shell with a real streaming conversation surface

---

## R7 Gateway Minimal Protocol

Completed `R7 / S2-1` by wiring the persisted workspace conversation boundary into a first OpenAI-compatible gateway surface and replacing the chat placeholder with a real completion flow.

Implemented in this round:

- extended shared contracts with a new `chat` module:
  - completion request / response DTOs
  - SSE chunk DTO
  - stop response DTO
  - model list DTO
  - gateway error DTO
- added gateway routes:
  - `GET /v1/models`
  - `POST /v1/chat/completions`
  - `POST /v1/chat/completions/:taskId/stop`
- kept auth aligned with existing workspace rules:
  - `Authorization: Bearer <sessionToken>` still gates access
  - inactive users are rejected before model execution
  - all chat gateway responses now carry `X-Trace-ID`
- bound completions onto the existing workspace launch records:
  - existing `conversation_id` resolves back to the user-owned workspace conversation
  - missing `conversation_id` will create a new workspace-backed conversation via launch
  - successful completions reuse the launch-created `run_id` and `trace_id`
  - run state now updates from `pending` to `succeeded` and stores completion outputs
- added minimal SSE protocol support:
  - `stream: true` returns `text/event-stream`
  - response format now emits `chat.completion.chunk` events plus `[DONE]`
  - `/stop` currently returns `soft`, leaving true interrupt semantics to `R8`
- changed the web chat surface:
  - `/chat/[conversationId]` is no longer a static bootstrap shell
  - the page now posts to `/v1/chat/completions`
  - successful sends append a real assistant message and refresh run status
- changed the same-origin gateway proxy:
  - upstream response bodies are now passed through instead of being buffered into text
  - `content-type`, `cache-control` and `x-trace-id` are preserved for future SSE UI work
- extended browser coverage:
  - workspace launch still redirects into `/chat/[conversationId]`
  - the chat page now sends a real prompt
  - the browser flow verifies the assistant reply and updated run status

Behavioral result:

- the workspace conversation shell is now attached to a real gateway protocol instead of only displaying bootstrap metadata
- `conversation_id`, `run_id` and `trace_id` created in `R6-B` now survive into model invocation responses
- the project now has a stable boundary for the next `S2-2 / S2-3` work:
  - progressive SSE rendering
  - stop UX
  - transcript persistence
  - deeper run tracking

## Validation

Validated this round with:

- `npm run type-check`
- `npm test`
- `npm run test:e2e`
- targeted protocol coverage:
  - `apps/gateway/src/routes/chat.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
  - `apps/web/src/lib/chat-client.test.ts`

Functional smoke against a running local gateway on `http://127.0.0.1:4211`:

- `POST /auth/register` -> `201`
- `POST /auth/login` -> `200`
- `POST /workspace/apps/launch` -> `200 conversation_ready`
- `POST /v1/chat/completions` -> `200 chat.completion`
- `X-Trace-ID` header == response `trace_id` == launch `traceId`

Browser regression result:

- `6 passed`
- the normal user flow now verifies:
  - workspace launch enters `/chat/[conversationId]`
  - sending a prompt hits `/v1/chat/completions`
  - the assistant response renders in the browser
  - run status updates to `succeeded`

## Next Step

Activate `R8`:

- turn the current SSE protocol output into progressive browser rendering
- add stop UX and message lifecycle states on the chat surface
- start persisting transcript/message history instead of keeping it only in page state

---

## R8 Streaming Conversation Surface

Completed `R8 / S2-2` by turning the minimal gateway protocol into a real streaming chat surface with stop semantics and persisted transcript state.

Implemented in this round:

- extended shared workspace contracts with persisted conversation messages:
  - `WorkspaceConversation.messages`
  - `WorkspaceConversationMessage`
  - message lifecycle states: `completed` / `streaming` / `stopped` / `failed`
- extended workspace services so run updates can also persist transcript history
- changed persistent workspace reads/writes:
  - `conversations.inputs.messageHistory` is now the transcript source of truth
  - launch-created conversations initialize with an empty `messageHistory`
  - conversation fetches now return restored message history in the workspace payload
- changed `/v1/chat/completions` streaming behavior:
  - SSE responses now stream incremental assistant chunks instead of only a terminal payload
  - active runs are tracked in-process so `/v1/chat/completions/:taskId/stop` can hard-stop a live stream
  - stream completion now persists transcript, message status and final run state on both success and stop
- changed the web chat client:
  - added direct SSE consumption through the same-origin gateway proxy
  - added stop request support
  - preserved `X-Trace-ID` / `trace_id` through the streaming path
- changed `/chat/[conversationId]`:
  - renders assistant output progressively while the stream is open
  - exposes `Stop response`
  - restores transcript history from the persisted workspace conversation payload on reload
  - shows per-message lifecycle status
- extended browser regression coverage:
  - first prompt verifies the normal streamed completion path
  - second prompt verifies stop behavior and visible `stopped` state

Behavioral result:

- the chat surface is no longer a blocking demo layered on top of an SSE-compatible API
- users now see token-by-token assistant rendering and can interrupt an active response
- conversation history survives page refresh because transcript state is read back from persisted workspace conversation data
- stop is now a true active-stream interrupt in the current gateway process, not only a soft acknowledgement

Implementation note:

- while wiring transcript persistence, `messageHistory` was briefly being written as a JSON string inside `conversations.inputs`
- the write path was corrected to persist a JSON array directly
- the read path was kept backward-compatible so previously stringified rows can still be parsed

## Validation

Validated this round with:

- `npm run type-check`
- `npm test`
- `npm run test:e2e`
- targeted protocol and persistence coverage:
  - `apps/gateway/src/routes/chat.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
  - `apps/gateway/src/routes/workspace.test.ts`
  - `apps/web/src/lib/chat-client.test.ts`
  - `apps/web/src/lib/apps-client.test.ts`

Suite result:

- `17` test files passed
- `98` tests passed

Browser regression result:

- `6 passed`
- the normal user flow now verifies:
  - workspace launch enters `/chat/[conversationId]`
  - the first prompt streams to completion
  - the second prompt is stopped through `/v1/chat/completions/:taskId/stop`
  - the UI surfaces `stopped`

Functional smoke against a running local gateway on `http://127.0.0.1:4212`:

- `POST /auth/register` -> `201`
- `POST /auth/login` -> `200`
- `POST /workspace/apps/launch` -> `200 conversation_ready`
- `POST /v1/chat/completions` with `stream: true` -> `200`
- first stream payload contained `chat.completion.chunk`
- `POST /v1/chat/completions/:taskId/stop` -> `200 hard`
- `GET /workspace/conversations/:conversationId` -> `200`
- restored conversation showed:
  - `run.status = stopped`
  - `messageCount = 2`
  - last assistant message `status = stopped`

## Next Step

Activate `R9`:

- add queryable run tracking and replay-oriented readback on top of the persisted conversation/run boundary
- expose richer run outputs / usage / status history for future multi-session recovery
- keep the next round aligned with `S2-3`, while leaving file upload and sharing for the following conversation iterations

---

## R9 Run Tracking And Replay

Completed `R9 / S2-3` by making workspace runs queryable, replayable and independent per completion instead of reusing a single launch-created run forever.

Implemented in this round:

- extended shared workspace contracts with:
  - `WorkspaceRunTrigger`
  - `WorkspaceRunUsage`
  - `WorkspaceRunSummary`
  - `WorkspaceRun`
  - `WorkspaceConversationRunsResponse`
  - `WorkspaceRunResponse`
- changed workspace services:
  - every new completion after the first launch-bound run now creates a fresh `run_id` and `trace_id`
  - both in-memory and persistent workspace services can now:
    - create a new run for an existing conversation
    - list runs for a conversation
    - read a single run detail payload
- changed persistent run readback:
  - conversation fetches now surface the latest run summary, including trigger, elapsed time, token count and finish timestamp
  - run detail payloads read back stored `inputs`, `outputs`, `usage`, `error`, app context and attributed group
- added workspace query routes:
  - `GET /workspace/conversations/:conversationId/runs`
  - `GET /workspace/runs/:runId`
- changed chat gateway behavior:
  - `POST /v1/chat/completions` now decides whether to reuse the initial pending launch run or create a new `chat_completion` run first
  - streaming metadata now carries `run_id` in addition to `conversation_id` and `trace_id`
  - completion persistence now stores `usage` inside run outputs so replay views can show prompt/completion totals
- changed the web chat surface:
  - stop now targets the active streamed run id rather than assuming the conversation’s previous run id
  - `/chat/[conversationId]` now loads run history and a selected run detail panel
  - the page renders replay snapshots for:
    - stored prompt input
    - stored assistant output
    - usage / timing / trigger metadata
- extended tests and browser coverage:
  - unit tests for new workspace client run endpoints
  - route tests for run list / run detail
  - chat tests for second completion creating a fresh run
  - persistent tests for reading back the second run from PostgreSQL
  - browser regression now checks run history and replay widgets after stop

Behavioral result:

- the first completion still uses the launch-created run so the handoff trace remains stable for the initial hop
- every later completion on the same conversation now gets its own run record
- run status, inputs, outputs and usage can be queried back independently from the conversation surface
- the browser can stop the correct active run even after a new run id is minted mid-session

## Validation

Validated this round with:

- `npm run type-check`
- `npm test`
- `npm run test:e2e`
- targeted run-tracking coverage:
  - `apps/gateway/src/routes/workspace.test.ts`
  - `apps/gateway/src/routes/chat.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
  - `apps/web/src/lib/apps-client.test.ts`
  - `apps/web/src/lib/chat-client.test.ts`

Suite result:

- `17` test files passed
- `102` tests passed

Browser regression result:

- `6 passed`
- the normal user flow now verifies:
  - launch enters `/chat/[conversationId]`
  - the first prompt succeeds on the initial run
  - the second prompt is stopped on a newly created run id
  - run history shows two runs
  - replay panels for `Prompt snapshot` and `Assistant output` render

Functional smoke against a running local gateway on `http://127.0.0.1:4213`:

- `POST /auth/register` -> `201`
- `POST /auth/login` -> `200`
- `POST /workspace/apps/launch` -> `200 conversation_ready`
- first `POST /v1/chat/completions` -> `200`
- second `POST /v1/chat/completions` -> `200`
- second completion returned a different `run_id` than launch / first completion
- `GET /workspace/conversations/:conversationId/runs` -> `200`
- `GET /workspace/runs/:runId` -> `200`
- run detail showed:
  - `triggeredFrom = chat_completion`
  - `usage.totalTokens > 0`
  - `conversationId` matched the launched conversation

## Next Step

Activate `R10`:

- move into the governance/admin closure while carrying forward the new conversation/run boundary
- keep file upload, sharing and finer-grained execution timelines as remaining `S2` backlog items
- reuse the queryable run surface as the base for later audit and platform observability work
