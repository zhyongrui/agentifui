# 2026-03-16 P3-A-10

Completed `P3-A-10` by adding a dedicated Playwright flow for admin source management plus retrieval-backed chat rendering.

Coverage added:

- `tests/e2e/knowledge-flows.spec.ts`
  - register/login as a tenant admin
  - open `/admin/sources`
  - create a group-scoped markdown source
  - mark the source `succeeded`
  - return to `/apps`
  - launch `Policy Watch` in `Research Lab`
  - send a retrieval-matching prompt
  - verify the source title appears in:
    - conversation citations
    - run-history source blocks

Validation:

- isolated stack:
  - gateway `4127`
  - web `3127`
- browser run:
  - `LD_LIBRARY_PATH=... PLAYWRIGHT_BASE_URL=http://127.0.0.1:3127 npx playwright test tests/e2e/knowledge-flows.spec.ts`

Continuity notes:

- the first browser assertion failed because the source title appeared multiple times in the conversation citation chips; the final assertion uses `.first()` instead of a strict single-match expectation
- the isolated servers were explicitly shut down after the run to avoid consuming exec-process slots on this host
- next active item is `P3-A-11`
