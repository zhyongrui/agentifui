# 2026-03-13

## R12-A Attachment Mainline

Completed `R12-A / S2` by turning workspace file uploads into a real conversation-bound capability instead of a placeholder backlog item.

Implemented in this round:

- shared contracts now include:
  - attachment metadata on workspace conversation messages
  - upload request / response DTOs
  - attachment size and MIME allow-list constants
- database and persistence now include:
  - new `workspace_uploaded_files` table
  - migration `0007_r12_a_workspace_uploads.sql`
  - persistent workspace file lookup bound to `conversation_id` and `user_id`
- gateway now includes:
  - local filesystem storage abstraction in `workspace-file-storage.ts`
  - `POST /workspace/conversations/:conversationId/uploads`
  - chat gateway file reference validation for `local_file`
  - attachment metadata persisted into conversation transcript and run inputs
- web now includes:
  - composer attachment picker on `/chat/[conversationId]`
  - upload-before-send flow using the workspace upload route
  - transcript rendering for uploaded attachments
  - run replay rendering for stored attachment metadata
- browser regression now includes:
  - upload `brief.txt`
  - send completion with the uploaded attachment
  - verify transcript and replay both show `Attached files`
  - keep the existing `Stop response` verification in the same flow

Useful continuity notes for future sessions:

- runtime package exports use `dist`, while TypeScript types come from `src`
  - after adding new runtime value exports to `packages/shared` or `packages/db`, `npm run type-check` can still pass while runtime values are `undefined`
  - the minimal fix is to rebuild the affected workspace packages before route tests or ad-hoc runtime smoke:
    - `npm run build --workspace @agentifui/shared`
    - `npm run build --workspace @agentifui/db`
- the new upload route is intentionally conversation-scoped:
  - `POST /workspace/conversations/:conversationId/uploads`
  - this keeps attachment authorization simple for Phase 1 because file ownership and replay both ride on the existing conversation boundary
- the current upload implementation is JSON + base64, not multipart
  - this was chosen to avoid adding a new Fastify multipart layer while keeping the `/api/gateway/*` proxy unchanged
  - if uploads need to grow beyond the current Phase 1 limit, `R13-B` or later should revisit multipart/object storage presign flows
- the Playwright attachment regression uses the stable DOM hook `#chat-attachment`
  - it uploads `Buffer.from('Policy attachment')`
  - it waits for `/api/gateway/workspace/conversations/:id/uploads`
  - it expects the transcript chip and replay section to both surface `brief.txt`
- public access/browser testing context from the prior round still applies
  - temporary `cloudflared tunnel --url http://127.0.0.1:3112 --no-autoupdate`
  - stable `80/443` reverse-proxy hardening remains deferred to `R13-B`

Validation performed in this round:

- `npm run build --workspace @agentifui/shared`
- `npm run build --workspace @agentifui/db`
- `npm run type-check`
- targeted Vitest:
  - `apps/gateway/src/routes/workspace.test.ts`
  - `apps/gateway/src/routes/chat.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
- full `npm test`
- full `npm run test:e2e`

Final suite result after `R12-A`:

- `21` test files passed
- `126` tests passed
- Playwright `6 passed`

Functional/browser verification summary:

- `POST /workspace/conversations/:conversationId/uploads` -> `200`
- `POST /v1/chat/completions` with `files: [{ type: 'local', file_id, transfer_method: 'local_file' }]` -> `200`
- `GET /workspace/conversations/:conversationId` now returns message-level attachment metadata
- `GET /workspace/runs/:runId` now returns `inputs.attachments`
- Playwright verified `upload -> completion -> transcript -> replay -> stop`

Follow-up planning change:

- `R12-A` is now complete
- the active next item in the rolling plan is `R12-B`
- next priority is conversation share contracts, persistence, create/revoke APIs and shared-access authorization

## R12-B Conversation Sharing

Completed `R12-B / S2` by turning conversation sharing from a plan item into a real login-protected, group-scoped read-only surface.

Implemented in this round:

- shared contracts now include:
  - conversation share DTOs, create/list/read responses and `read_only` access mode
  - share lifecycle status values `active` / `revoked`
  - audit actions for share create, access and revoke
- database and persistence now include:
  - new `workspace_conversation_shares` table
  - migration `0008_r12_b_conversation_shares.sql`
  - owner-only share create/list/revoke queries plus shared transcript lookup by `share_id`
- gateway now includes:
  - `GET /workspace/conversations/:conversationId/shares`
  - `POST /workspace/conversations/:conversationId/shares`
  - `DELETE /workspace/conversations/:conversationId/shares/:shareId`
  - `GET /workspace/shares/:shareId`
  - audit writes for `workspace.conversation_share.created`, `.accessed`, `.revoked`
- web now includes:
  - a share management panel on `/chat/[conversationId]`
  - create/revoke controls for group-scoped read-only shares
  - `Open shared view` links that target `/chat/shared/[shareId]`
  - a dedicated read-only shared transcript page with attachments but no composer
- browser regression now includes:
  - owner creates a conversation in `Research Lab`
  - owner creates a read-only share for `grp_research`
  - a different member logs in and opens the shared URL
  - the shared transcript loads and the message composer is absent

Useful continuity notes for future sessions:

- shared conversation access is not public/anonymous
  - the consumer must still hold a valid workspace session
  - `GET /workspace/shares/:shareId` then enforces active membership in the shared target group
  - a revoked share or a user outside the group will get a forbidden/not-found response instead of a degraded partial view
- share URLs are stable app routes, not raw gateway links
  - the canonical user-facing path is `/chat/shared/:shareId`
  - the Next.js page calls the gateway through the existing `/api/gateway/*` proxy, so no extra public routing is needed for Phase 1
- share management is intentionally owner-scoped in this phase
  - only the conversation owner can list/create/revoke shares
  - readers only get the shared transcript surface, not share metadata editing controls
- the shared transcript page reuses the same bubble styling as the main chat surface
  - when asserting transcript content in Playwright, scope to `.chat-bubble.user p` or another bubble-local locator
  - plain `getByText(...)` can hit both the explanatory copy and the transcript body once prompts resemble page text
- the earlier public browser-access note still applies unchanged
  - temporary manual access can still be provided with `cloudflared tunnel --url http://127.0.0.1:3112 --no-autoupdate`
  - stable `80/443` ingress hardening remains deferred to `R13-B`

Validation performed in this round:

- `npm run type-check`
- full `npm test`
- full `npm run test:e2e`

Final suite result after `R12-B`:

- `21` test files passed
- `127` tests passed
- Playwright `7 passed`

Functional/browser verification summary:

- owner share list/create/revoke routes are covered in `apps/gateway/src/routes/workspace.test.ts`
- Playwright verified `owner creates share -> reader opens /chat/shared/:shareId -> transcript is visible -> composer is absent`
- audit coverage now includes share create, access and revoke events

Follow-up planning change:

- `R12-B` is now complete
- the active next item in the rolling plan is `R12-C`
- next priority is timeline DTOs, persisted run timeline events and recent conversation history

## R12-C Timeline And History

Completed `R12-C / S2` by turning run replay into a persisted timeline/history surface instead of a single deep-linked conversation page.

Implemented in this round:

- shared contracts now include:
  - `WorkspaceRunTimelineEvent`
  - `WorkspaceConversationListItem`
  - `GET /workspace/conversations` list response DTO
- database and persistence now include:
  - new `run_timeline_events` table
  - migration `0009_r12_c_run_timeline.sql`
  - persisted timeline reads on `GET /workspace/runs/:runId`
- gateway now includes:
  - timeline event appends for run create, input, start, output, succeed, fail, stop and stop-requested
  - `GET /workspace/conversations` with `appId` / `groupId` / `q` / `limit`
- web now includes:
  - a new `/chat` history page
  - history filters by app, group and free-text query
  - timeline rendering inside `/chat/[conversationId]` run replay
- browser regression now includes:
  - open `/chat`
  - filter recent conversation history
  - jump back into a stored conversation
  - verify `Run timeline` is visible

Useful continuity notes for future sessions:

- `run_timeline_events` is now the source of truth for replay-stage history
  - `run_created` is written on launch and on new chat-completion run creation
  - `stop_requested` is written by `POST /v1/chat/completions/:taskId/stop`
  - the final run status events are appended by `updateConversationRunForUser(...)`
- `/chat` is now the intended non-deep-link entry point for prior conversations
  - it calls `GET /workspace/conversations`
  - free-text search currently matches conversation title plus serialized `inputs.messageHistory`
- first completion on a newly launched conversation still reuses the launch-created run
  - that run keeps `triggeredFrom = app_launch`
  - the second completion on the same conversation is the first one that creates a distinct `chat_completion` run
- Playwright selectors around attachment and transcript text need scoped locators
  - attachment filenames can now appear in both draft chips and replay output
  - use `.chat-attachment-list` or `.chat-bubble.*` scoped locators instead of plain `getByText(...)`

Validation performed in this round:

- `npm run build --workspace @agentifui/shared`
- `npm run build --workspace @agentifui/db`
- `npm run type-check`
- targeted Vitest:
  - `apps/web/src/lib/apps-client.test.ts`
  - `apps/gateway/src/routes/workspace.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
- full `npm test`
- full `npm run test:e2e`

Final suite result after `R12-C`:

- `21` test files passed
- `129` tests passed
- Playwright `8 passed`

Functional/browser verification summary:

- `GET /workspace/conversations` now returns recent history items ordered newest-first
- `GET /workspace/runs/:runId` now returns persisted timeline events
- Playwright verified `launch -> completion -> /chat history -> filtered reopen -> timeline replay`

Follow-up planning change:

- `R12-C` is now complete
- the active next item in the rolling plan is `R12-D`
- next priority is quota persistence, real usage accounting and chat-side quota visibility

## R12-D Persisted Quota Closure

Completed `R12-D / S1-3` by replacing fixture-only quota state with persisted quota limits plus actual launch/run usage accounting.

Implemented in this round:

- shared / audit contracts now include:
  - `workspace.quota.launch_blocked`
  - `workspace.quota.usage_recorded`
- database and persistence now include:
  - new `workspace_quota_limits` table
  - migration `0010_r12_d_workspace_quota_limits.sql`
  - quota read model built from persisted scope limits plus live launch/run usage aggregation
- gateway now includes:
  - real quota snapshot generation in both in-memory and persistent workspace services
  - launch blocking audit writes when quota is exceeded
  - completion usage audit writes after blocking and streaming completions finish
- web now includes:
  - `/chat/[conversationId]` quota context cards driven from the real workspace catalog
  - `/apps` quota cards now transparently ride on the same persisted quota data source
- browser regression now includes:
  - quota cards visible on the conversation surface
  - existing launch / stop / history flows running against real quota snapshots

Useful continuity notes for future sessions:

- quota limits are seeded lazily per tenant/user/group
  - the seed table is `workspace_quota_limits`
  - default seed values intentionally preserve the earlier baseline UX:
    - tenant base used `820 / 1000`
    - user base used `610 / 1000`
    - product group base used `930 / 1000`
    - research group base used `760 / 1000`
    - security group base used `540 / 1000`
- live quota usage is additive on top of those baselines
  - each app launch adds `app.launchCost`
  - each completion adds `ceil(totalTokens / 25)` credits
  - terminal run statuses counted for quota are `succeeded`, `failed`, `stopped`
- chat page quota cards are refreshed via a fresh workspace catalog fetch inside conversation bootstrap
  - if a future session optimizes this away, keep the group attribution consistent with `conversation.activeGroup.id`
- Playwright is sensitive to infra slowness in production-mode Next builds
  - `Run status = succeeded` and MFA setup both now use explicit gateway waits / 60s timeouts
  - if later flows start flaking again, prefer waiting on the gateway call before loosening generic text assertions
- public browser continuity from earlier rounds still applies
  - ad-hoc external access can still use `cloudflared tunnel --url http://127.0.0.1:3112 --no-autoupdate`
  - stable `80/443` ingress hardening is still the planned `R13-B` follow-up

Validation performed in this round:

- `npm run build --workspace @agentifui/shared`
- `npm run build --workspace @agentifui/db`
- `npm run type-check`
- targeted Vitest:
  - `apps/gateway/src/routes/workspace.test.ts`
  - `apps/gateway/src/routes/chat.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts`
- full `npm test`
- full `npm run test:e2e`

Final suite result after `R12-D`:

- `21` test files passed
- `129` tests passed
- Playwright `8 passed`

Functional/browser verification summary:

- `GET /workspace/apps` now returns quota cards backed by `workspace_quota_limits` + live usage
- `POST /workspace/apps/launch` blocks and audits when quota would be exceeded
- `POST /v1/chat/completions` records completion quota usage into audit and updates subsequent catalog reads
- Playwright verified conversation pages show `Quota context` while the existing launch / chat / stop flows stay green

Follow-up planning change:

- `R12-D` is now complete
- the active next item in the rolling plan is `R13-B`
- next priority is stable public ingress, process management and deployment hardening

## R13-B1 And R13-B4 Deployment Baseline

Completed the first executable baseline for `R13-B1` and `R13-B4` so future rounds no longer need to reconstruct public ingress and process management from scattered dev-log notes.

Implemented in this round:

- added repo-managed deployment assets:
  - `deploy/nginx/agentifui.conf`
  - `deploy/systemd/agentifui-gateway.service`
  - `deploy/systemd/agentifui-web.service`
- added a deployment guide:
  - `docs/guides/R13-B_DEPLOYMENT_BASELINE.md`
- updated the long-term plan so the next active `R13-B` work is:
  - HTTPS and formal `/api/gateway` ingress hardening
  - env template closure
  - repeatable public-browser smoke

Useful continuity notes for future sessions:

- the practical production shape is now fixed in writing:
  - `nginx` on `80/443`
  - Next.js on `127.0.0.1:3112`
  - Gateway on `127.0.0.1:4214`
  - `/api/gateway/*` reverse-proxied to the gateway
- `proxy_buffering off` is called out explicitly for `/api/gateway/`
  - keep that in place or streaming chat will regress behind the reverse proxy
- the existing temporary workaround is still documented but demoted:
  - `cloudflared tunnel --url http://127.0.0.1:3112 --no-autoupdate`
  - use it only when `nginx` cannot be changed in the current shell context
- the new `systemd` units intentionally prefer clarity over host-specific pinning
  - they use `npm run start --workspace ...`
  - if a future host requires an absolute node binary, update only the unit files rather than rewriting the guide text

Validation performed in this round:

- documentation/config review only
- no code/test rerun was required because this round only added deployment assets and plan/log updates after the already-green `R12-C` / `R12-D` commit

Follow-up planning change:

- `R13-B1` / `R13-B4` baseline docs are now complete
- the active next item in the rolling plan is `R13-B2` / `R13-B3`
- next priority is formal HTTPS/public ingress closure on top of the new baseline files

## R13-B2 Through R13-C6 Ingress, CI, Observability And Release Closure

Completed the remaining `R13-B` and `R13-C` infrastructure backlog so future rounds can keep shipping feature work without rebuilding deploy/test/release plumbing first.

Implemented in this round:

- gateway / web runtime now include:
  - structured `request.completed` logs on every gateway response
  - `GET /metrics` Prometheus-style text output on the gateway
  - same-origin Next proxy support for `DELETE /api/gateway/*`, which unblocks revoke flows behind the public origin
- deployment and release assets now include:
  - `deploy/nginx/agentifui-https.conf`
  - `docs/guides/R13_RELEASE_CHECKLIST.md`
  - `scripts/smoke-deploy.mjs`
  - `scripts/public-browser-smoke.mjs`
- environment templates now include:
  - updated development `.env.example`
  - new `.env.test.example`
  - new `.env.production.example`
- CI now includes separate GitHub Actions jobs for:
  - `typecheck`
  - `build`
  - `db-validate`
  - `unit`
  - `e2e`

Useful continuity notes for future sessions:

- the public-origin contract is now explicit and reusable:
  - browser traffic should hit `https://host/...`
  - gateway traffic should stay same-origin under `https://host/api/gateway/...`
  - local smoke used the same pattern with `http://127.0.0.1:3116` and `http://127.0.0.1:4216`
- `smoke:browser` must keep reusing `scripts/prepare-playwright-runtime.mjs`
  - on this server, skipping that setup reproduced `libatk-1.0.so.0` missing-library failures
  - the browser smoke script now mirrors the same runtime bootstrap used by `npm run test:e2e`
- database reset and gateway startup must be sequential when both can touch migrations
  - running `npm run db:reset` in parallel with gateway startup reproduced a PostgreSQL race on `_agentifui_migrations`
  - the failure surfaced as `duplicate key value violates unique constraint "pg_type_typname_nsp_index"`
  - future deploy/reset automation should always finish reset/migrate before starting the gateway
- Next 16 production builds still emit the stale `baseline-browser-mapping` warning
  - it is currently non-blocking for build, e2e and smoke
  - treat it as dependency hygiene, not as a deploy blocker
- the gateway proxy route must keep `DELETE` parity with the upstream gateway
  - otherwise share revoke and admin grant revoke work through direct gateway calls but fail behind `/api/gateway`

Validation performed in this round:

- `npm run type-check`
- targeted Vitest:
  - `apps/gateway/src/app.test.ts`
  - `apps/web/src/app/api/gateway/[...path]/route.test.ts`
  - `apps/gateway/src/config/env.test.ts`
- full `npm test`
- full `npm run test:e2e`
- local same-origin smoke on production builds:
  - `SMOKE_BASE_URL=http://127.0.0.1:3116 npm run smoke:deploy`
  - `PUBLIC_BASE_URL=http://127.0.0.1:3116 npm run smoke:browser`

Final suite result after `R13-B2` through `R13-C6`:

- `21` test files passed
- `131` tests passed
- Playwright `8 passed`
- local `smoke:deploy` succeeded with launch + completion + conversation readback
- local `smoke:browser` succeeded with `login -> /apps -> /chat`

Functional/release verification summary:

- `GET /metrics` now exposes gateway request counters and latency sums/max per route
- `.github/workflows/ci.yml` now validates typecheck, build, DB migrate/reset, unit and e2e separately
- the HTTPS/nginx docs now cover certbot bootstrap, renew dry-run and same-origin `/api/gateway` ingress
- release operators now have a documented rollback path plus repeatable smoke commands

Follow-up planning change:

- `R13-B` is now complete
- `R13-C` is now complete
- the active next item in the rolling plan is `R13-A1` / `R13-A2`
- the remaining Phase 1 focus is platform admin and tenant lifecycle

## R13-A1 Through R13-A4 Platform Tenant Lifecycle

Completed the first platform-admin lifecycle block so Phase 1 no longer stops at tenant-scoped governance. Root admins can now inventory tenants, create a new tenant with bootstrap state, and suspend/reactivate that tenant through the persisted gateway boundary.

Implemented in this round:

- shared contracts now include:
  - platform tenant summary DTOs
  - tenant create request/response
  - tenant status update request/response
  - audit actions for tenant create / suspend / reactivate
- gateway admin routes now include root-admin-only endpoints:
  - `GET /admin/tenants`
  - `POST /admin/tenants`
  - `PUT /admin/tenants/:tenantId/status`
- persistent admin service now creates real tenants by seeding:
  - tenant row
  - tenant-scoped groups and apps
  - default group grants and role grants
  - bootstrap admin user, membership and roles
  - quota limits
  - invitation token for `/auth/invitations/accept`
- auth persistence now maps tenant suspension into the effective user status returned to the rest of the app:
  - password login blocked
  - SSO callback blocked
  - MFA verify blocked
  - existing session reads fall through the same access boundary
- web `/admin/tenants` now exposes:
  - real tenant inventory cards
  - create-tenant form
  - last bootstrap invite details
  - suspend / reactivate actions
- test runtime was hardened:
  - `npm test` is now split into `test:unit` and `test:persistence`
  - DB migration tracking now always uses `public._agentifui_migrations`
  - `db:reset` can recreate the migration table if it is temporarily missing

Useful continuity notes for future sessions:

- new-tenant workspace resources cannot reuse the legacy seed helper directly
  - the old helper inserts globally fixed `grp_*` / `app_*` ids
  - platform tenant creation now copies those defaults into tenant-scoped ids like `tenant-acme:grp_research`
- bootstrap tenant admin activation intentionally reuses the existing invitation accept flow
  - the admin create response returns `inviteToken` and `/invite/accept?...`
  - the invited pending user is activated in place rather than creating a duplicate account
- tenant suspend enforcement is centralized in auth persistence, not duplicated route-by-route
  - `findUserByEmail` / `findUserById` join `tenants`
  - tenant status is folded into the effective `AuthUser.status`
  - existing route guards that already require `active` now enforce tenant suspension automatically
- test orchestration on this repo must stay sequential when persistence and E2E both touch Postgres
  - running persistence tests in parallel with `npm run test:e2e` caused false failures because `db:reset` tears down shared tables underneath Vitest
  - keep `npm test` and `npm run test:e2e` strictly sequential
- the migration-table recovery in `packages/db/src/runtime.ts` is intentional
  - it guards the reset/migrate workflow after seeing transient `42P01` on `_agentifui_migrations`
  - do not revert it as “cleanup” unless the reset pipeline is redesigned end-to-end

Validation performed in this round:

- `npm run type-check`
- targeted Vitest:
  - `apps/gateway/src/routes/admin.test.ts`
  - `apps/web/src/lib/admin-client.test.ts`
  - `apps/gateway/src/routes/auth-persistence.test.ts -t "persists sessions and revokes them on logout across app restarts"`
  - `apps/gateway/src/routes/auth-persistence.test.ts -t "applies role allow, user allow, and explicit deny precedence when building the workspace catalog"`
  - `apps/gateway/src/routes/auth-persistence.test.ts -t "returns persisted admin governance data for tenant admins"`
  - `apps/gateway/src/routes/auth-persistence.test.ts -t "persists platform tenant lifecycle and enforces suspended tenant access boundaries"`
- full `npm test`
- full `npm run test:e2e`
- `DATABASE_URL=postgresql://agentifui:agentifui@localhost:5432/agentifui npm run db:reset`

Final suite result after `R13-A1` through `R13-A4`:

- `21` test files passed
- `135` tests passed
- Playwright `9 passed`

Functional/browser verification summary:

- root admins can open `/admin/tenants`, create a tenant, see the returned bootstrap invite, suspend it, then reactivate it
- invited tenant admins can activate through `/invite/accept` and then access their seeded tenant workspace
- suspended tenants are blocked from login and persisted workspace access until reactivated

Follow-up planning change:

- `R13-A1` through `R13-A4` are now complete
- the active next item in the rolling plan is `R13-A5` / `R13-A6`
- once `R13-A5` / `R13-A6` land, `Phase 1` is ready for closing regression and the next long-range plan

## R13-A5 Through R13-A6 Platform Audit Scope And Root Admin Boundaries

Completed the final Phase 1 platform-governance block so the admin surface now distinguishes tenant-scoped governance from platform-scoped governance at both the API and browser levels.

Implemented in this round:

- shared admin contracts now include:
  - `AdminViewerCapabilities`
  - `AdminContextResponse`
  - platform-aware audit filters: `scope`, `tenantId`
  - audit response aggregates: `countsByTenant`, `highRiskEventCount`
  - `tenantName` on audit event summaries
- gateway admin routes now include:
  - `GET /admin/context`
  - stricter platform audit permission checks on `GET /admin/audit`
  - the same scope enforcement on `GET /admin/audit/export`
  - CSV export columns for `tenant_id` and `tenant_name`
- persistent admin audit reads now support:
  - tenant scope for tenant admins
  - platform scope for root admins
  - tenant-name hydration when old audit rows do not carry the label directly
  - high-risk event counting and tenant spread aggregation
- web admin UX now distinguishes capabilities cleanly:
  - tenant admins no longer see the `Tenants` nav item
  - `/admin/audit` boots from the audit payload capability first, then lazily pulls tenant inventory only for platform viewers
  - root admins see platform summary badges, scope selector, tenant selector and tenant spread
- browser regression was tightened in the same round:
  - tenant-admin audit waits now include explicit `scope=tenant`
  - MFA login flow now waits for `/auth/login` before asserting `/auth/mfa`
  - root-admin platform audit assertions now use precise locators for summary badges and tenant-spread tags
- the DB reset pipeline was hardened while validating this work:
  - `packages/db/src/scripts/reset.ts` now resets the schema and reopens a fresh DB connection before running migrations
  - this removes the transient `_agentifui_migrations` catalog conflict seen when reset and migrate shared a single Postgres session

Useful continuity notes for future sessions:

- `/admin/audit` should remain the source of truth for platform capability on first page load
  - an earlier version that fetched `/admin/context` first caused the audit page to stall on `Loading admin audit...` during browser tests
  - the current flow uses audit data first, then enriches tenant labels via `/admin/tenants`
- root-admin and tenant-admin boundaries are now explicit and should stay centralized:
  - nav visibility comes from `/admin/context`
  - audit scope authorization is enforced server-side in `apps/gateway/src/routes/admin.ts`
  - tenant admins must never be able to request `scope=platform` or foreign `tenantId`
- `npm test` and `npm run test:e2e` must stay sequential on this repo
  - running them in parallel against the same Postgres DB reproduced false failures while `db:reset` dropped shared tables under the persistence tests
- `db:reset` should keep the “fresh connection after reset” behavior
  - the old single-session flow intermittently failed with `_agentifui_migrations` duplicate catalog errors during schema recreation
- local/manual gateway or Next processes that share the same Postgres DB should be stopped before long E2E runs when possible
  - earlier leftover manual test services were not the only cause, but they increased the chance of dirty reset/migration timing during browser validation

Validation performed in this round:

- `npm run type-check`
- `npm test`
- `npm run test:e2e`

Final suite result after `R13-A5` through `R13-A6`:

- `21` test files passed
- `135` tests passed
- Playwright `9 passed`

Functional/browser verification summary:

- root admins can open `/admin/tenants`, create/suspend/reactivate a tenant, then pivot into `/admin/audit` with `Scope: platform`
- tenant admins can still open `/admin/audit`, but only within tenant scope and without platform navigation affordances
- platform audit now exposes tenant spread, high-risk summary and tenant-filtered event replay from persisted audit history

Follow-up planning change:

- `R13-A5` and `R13-A6` are now complete
- `R13-A` is fully complete
- `Phase 1` feature delivery is complete
- the active next items are:
  - Phase 1 closeout regression and deploy review
  - Phase 2 long-range task-board freeze
  - Phase 2 first implementation batch

## P2-A1 Through P2-A2 Conversation Feedback And Message Actions

Started Phase 2 implementation from the new rolling task board and completed the first two transcript-focused tasks so the conversation surface now supports durable assistant feedback and basic message actions.

Implemented in this round:

- shared workspace contracts now include:
  - `WorkspaceConversationMessage.feedback`
  - `WorkspaceConversationMessageFeedbackRequest`
  - `WorkspaceConversationMessageFeedbackResponse`
- shared auth audit unions now include:
  - `workspace.message.feedback.updated`
  - `conversation_message`
- gateway workspace routes now include:
  - `PUT /workspace/conversations/:conversationId/messages/:messageId/feedback`
- both workspace service implementations now support:
  - assistant-message feedback updates in memory
  - persisted feedback writes into `conversations.inputs.messageHistory`
- web `/chat/[conversationId]` now supports:
  - `Helpful` / `Needs work` feedback toggles on completed assistant replies
  - `Copy`
  - `Quote`
  - `Retry`
  - `Regenerate`
- browser regression now verifies:
  - feedback survives reload
  - retry restores the original user prompt into the composer
  - quote appends assistant content into the composer

Useful continuity notes for future sessions:

- assistant feedback is stored on the conversation transcript itself, not on the run record
  - future transcript search/filter work should read it from `WorkspaceConversationMessage.feedback`
- `Regenerate` is intentionally scoped to the latest completed assistant reply
  - this avoids unsupported branch semantics before artifact/runtime work lands
- `Retry` restores saved workspace attachments back into the composer
  - this currently reuses the attachment metadata that already lives on the prior user message
- root-admin browser checks should explicitly wait for `/api/gateway/admin/context`
  - the `Tenants` nav item is capability-driven and can arrive after the initial `/admin/users` shell renders

Validation performed in this round:

- `npm run type-check`
- `npm test`
- `npm run test:e2e`

Final suite result after `P2-A1` and `P2-A2`:

- `21` test files passed
- `139` tests passed
- Playwright `9 passed`

Functional/browser verification summary:

- assistant feedback toggles persist after page reload and are queryable through the workspace conversation route
- transcript message actions work on the production chat page running behind same-origin `/api/gateway`
- root admins still retain tenant inventory access after the new chat/browser checks

Follow-up planning change:

- `P2-A1` is complete
- `P2-A2` is complete
- the active next item is `P2-A3`
- the next queued follow-on items are:
  - `P2-B1`
  - `P2-B2`
  - `P2-C1`
