# 2026-03-18

## Summary
- added the first real tenant policy-control surface across shared contracts, gateway services/routes, and the web admin workspace
- completed `P4-F-02`, `P4-F-03`, `P4-F-04`, and `P4-F-05`
- left `P4-F-01` active because runtime-mode governance is now editable in admin but is not yet the source of truth for provider routing

## What Changed
- shared contracts:
  - added `policyPack.retrievalMode`
  - expanded audit enums with:
    - `workspace.policy.flagged`
    - `workspace.policy.blocked`
    - `admin.policy.simulated`
    - `admin.policy.exception.created`
    - `admin.policy.exception.reviewed`
    - `policy_evaluation`
    - `policy_exception`
- gateway:
  - added `apps/gateway/src/services/policy-engine.ts`
  - added `apps/gateway/src/services/policy-service.ts`
  - added `apps/gateway/src/services/persistent-policy-service.ts`
  - added `apps/gateway/src/routes/admin-policy.ts`
  - wired policy service into `buildApp()`
  - enforced export policy on:
    - `/admin/usage/export`
    - `/admin/audit/export`
    - `/admin/billing/export`
  - enforced retrieval policy on `/v1/chat/completions` by evaluating the latest prompt and zeroing retrieval matches when blocked
  - persisted/replayed policy evaluation traces through `tenant_policy_evaluations`
  - persisted/reviewed exceptions through `tenant_policy_exceptions`
- web:
  - added `/admin/policy`
  - added admin client helpers for:
    - `fetchAdminPolicy()`
    - `simulateAdminPolicy()`
    - `createAdminPolicyException()`
    - `reviewAdminPolicyException()`
  - added `Policy` into the admin section nav
  - extended `/admin/identity` so governance editing now covers:
    - `retrievalMode`
    - `exportMode`
    - `retentionMode`

## Validation
- `npm run type-check --workspace @agentifui/gateway`
- `npm run type-check --workspace @agentifui/web`
- `npx vitest run apps/gateway/src/routes/admin-policy.test.ts`
- `npx vitest run apps/gateway/src/routes/admin-identity.test.ts`
- `npx vitest run apps/web/src/lib/admin-client.test.ts`
- `npm run build`

Observed results:

- gateway type-check passed
- web type-check passed
- `admin-policy.test.ts`: `3 passed`
- `admin-identity.test.ts`: `2 passed`
- `admin-client.test.ts`: `13 passed`
- build passed and `/admin/policy` is present in the generated app routes

## Infra / Runtime
- no new migrations were required in this round; the work reuses:
  - `tenant_policy_exceptions`
  - `tenant_policy_evaluations`
  - `operator_annotations`
  from `0022_p4_e_f_g_ops.sql`
- no browser/public QA was run in this round

## Continuity Notes
- `exportMode=approval_required` is currently enforced as a hard block, not a separate approval queue
  - this is intentional for now because the queue/reviewer flow does not exist yet
  - when `P4-F-01`/later governance work continues, convert this into a real approval workflow instead of a generic deny
- retrieval policy is enforced inside `apps/gateway/src/routes/chat.ts`
  - the route still builds the retrieval query/result shape for consistency
  - if policy outcome is `blocked`, the persisted run input keeps the trace but the retrieval `matches` array is emptied before runtime invocation
- `/admin/policy` is the current operator entry point for:
  - recent evaluation traces
  - policy simulations
  - detector exception CRUD/review
- `/admin/identity` remains the place where the policy-pack values themselves are edited
- runtime-mode governance is not fully closed yet
  - admins can edit `policyPack.runtimeMode`
  - provider routing still primarily resolves tenant runtime mode through the existing runtime routing path
  - keep `P4-F-01` open until runtime-mode enforcement is fully driven by tenant governance
- large-file `apply_patch` truncation risk on this host still applies
  - safe recovery remains: `git show HEAD:path > path`

## Git State
- pending commit(s): policy-control batch for `P4-F-02..05`
- push status: not yet pushed at the time this log entry was written

## Next Active Item
- `P4-F-06` evidence export bundles for audit/compliance review

## Follow-up Summary
- completed `P4-F-01`
- completed `P4-G-01`, `P4-G-05`, `P4-G-07`, `P4-G-09`, and `P4-G-11`
- added the first real admin observability surface and closed runtime-mode governance so policy-pack `runtimeMode` now affects provider routing decisions

## Follow-up Changes
- gateway:
  - added `apps/gateway/src/routes/admin-observability.ts`
  - added `apps/gateway/src/services/admin-observability-service.ts`
  - added `apps/gateway/src/services/persistent-admin-observability-service.ts`
  - wired `adminObservabilityService` into `buildApp()`
  - added `/admin/observability`
  - routed `policyPack.runtimeMode` from tenant governance into `workspace-runtime` provider selection
- web:
  - added `/admin/observability`
  - added admin client helpers for:
    - `fetchAdminObservability()`
    - `createAdminObservabilityAnnotation()`
  - added `Observability` into the admin section nav
- docs:
  - added `docs/guides/P4-G_OBSERVABILITY_OPERATIONS.md`

## Follow-up Validation
- `npm run type-check --workspace @agentifui/gateway`
- `npm run type-check --workspace @agentifui/web`
- `npx vitest run apps/gateway/src/routes/admin-observability.test.ts apps/gateway/src/routes/chat.test.ts apps/gateway/src/services/workspace-runtime.test.ts`
- `npx vitest run apps/web/src/lib/admin-client.test.ts`
- `npm run build`

Observed results:

- gateway type-check passed
- web type-check passed
- `admin-observability.test.ts`, `chat.test.ts`, `workspace-runtime.test.ts`: `25 passed`
- `admin-client.test.ts`: `14 passed`
- build passed and `/admin/observability` is present in the generated app routes

## Follow-up Continuity Notes
- `apps/gateway/src/routes/chat.ts` now reads tenant governance through `policyService.getOverviewForUser()`
  - `policyPack.runtimeMode` is passed into `runtimeService.invoke(...)`
  - provider routing now prefers the per-invocation `tenantRuntimeMode` over env fallback
- `/admin/observability` is the current operator entry point for:
  - auth / launch / chat latency SLI
  - run success rate
  - route metrics
  - alert cards with runbook links
  - incident timeline stitched from audit events, runtime degradation, and operator annotations
- alert runbook links currently target `docs/guides/P4-G_OBSERVABILITY_OPERATIONS.md`
  - keep these anchors stable if the guide is edited:
    - `#triage-gateway-5xx`
    - `#runtime-degraded`
    - `#sli-response`
- runtime governance is now closed enough to mark `P4-F-01` done
  - remaining governance work should move to `P4-F-06..12`

## Compliance Follow-up Summary
- completed `P4-F-07`
- completed `P4-F-10`
- added legal-hold delete blocking for conversations and added detector-type filtering to admin audit

## Compliance Follow-up Changes
- gateway:
  - `/workspace/conversations/:conversationId` now blocks `status=deleted` when tenant legal hold is enabled
  - the blocked delete path records `workspace.conversation.delete_blocked`
  - `/admin/audit` now accepts `detectorType`
  - both memory and persistent admin audit services now derive detector types from:
    - `policy.detectorMatches[*].detector`
    - `safetySignals[*].category`
    - `categories[]`
- web:
  - `/admin/audit` now exposes a detector filter alongside the existing severity filter
  - audit client query builder now forwards `detectorType`

## Compliance Follow-up Validation
- `npx vitest run apps/gateway/src/routes/workspace.test.ts apps/gateway/src/routes/admin.test.ts -t "legal hold|parses admin audit filters|validates admin audit query parameters|loads admin summaries with audit visibility"`
- `npx vitest run apps/web/src/lib/admin-client.test.ts`
- `npm run type-check --workspace @agentifui/gateway`
- `npm run type-check --workspace @agentifui/web`
- `npm run build`

Observed results:

- workspace/admin route subset: `3 passed`
- `admin-client.test.ts`: `14 passed`
- gateway type-check passed
- web type-check passed
- build passed

## Compliance Follow-up Continuity Notes
- legal hold behavior now has three layers:
  - cleanup already expands retention windows to effectively no-op
  - backup/restore keeps governance metadata as-is
  - route-level conversation deletion is now explicitly blocked
- `detectorType` is intentionally derived from payload content instead of a new persisted column
  - this keeps old audit rows queryable without a migration
  - supported filter values are:
    - `secret`
    - `pii`
    - `regulated_term`
    - `exfiltration_pattern`
    - `prompt_injection`
    - `data_exfiltration`
    - `policy_violation`

## Evidence Export Summary
- completed `P4-F-06`
- added audit evidence-bundle export with trace summaries for compliance review

## Evidence Export Changes
- gateway:
  - added `GET /admin/audit/evidence-bundle`
  - evidence export reuses audit filters and export policy enforcement
  - response now includes `traceSummaries[]` with:
    - `traceId`
    - `runIds`
    - `conversationIds`
    - `eventCount`
    - `highestLevel`
    - `actions`
    - `detectorTypes`
    - `firstOccurredAt`
    - `lastOccurredAt`
- web:
  - added `exportAdminAuditEvidenceBundle()`
  - `/admin/audit` now has a third export action for evidence bundles

## Evidence Export Validation
- `npx vitest run apps/gateway/src/routes/admin.test.ts -t "parses admin audit filters|exports filtered admin audit events as JSON attachments|exports audit evidence bundles with trace summaries"`
- `npx vitest run apps/web/src/lib/admin-client.test.ts`
- `npm run type-check --workspace @agentifui/gateway`
- `npm run type-check --workspace @agentifui/web`
- `npm run build`

Observed results:

- targeted `admin.test.ts`: `3 passed`
- `admin-client.test.ts`: `15 passed`
- gateway type-check passed
- web type-check passed
- build passed

## Evidence Export Continuity Notes
- evidence bundles are intentionally derived from existing audit payloads
  - there is no new evidence table or migration in this round
  - this keeps historical audit rows exportable immediately
- trace summaries only include events with a non-null `context.traceId`
  - if a tenant’s older audit rows did not record trace ids, the evidence bundle may have an empty `traceSummaries` array while still including `events`
- evidence export filenames are rewritten from `admin-audit-*.json` to `admin-audit-evidence-*.json`

## Next Active Item
- `P4-F-08` policy precedence tests across tenant, group, app, and runtime scopes
