# 2026-03-16 P3-A-06

Completed `P3-A-06` by backfeeding retrieval matches into the existing citation/source-block model.

Key wiring:

- `packages/shared/src/apps/contracts.ts`
  - added `WorkspaceSourceBlockKind = "knowledge"`
- `apps/gateway/src/services/workspace-runtime.ts`
  - `buildAssistantSources(...)` now appends retrieval-backed `knowledge` blocks
  - knowledge blocks reuse:
    - `title`
    - `preview`
    - `sourceUri`
    - `sourceId/chunkId/score/headingPath` metadata
- `apps/gateway/src/routes/chat.test.ts`
  - blocking completions now assert the response/run replay includes `Policy Watch handbook` as a knowledge-backed citation/source block

Validation:

- `npm run type-check`
- `npx vitest run apps/gateway/src/services/workspace-runtime.test.ts apps/gateway/src/routes/chat.test.ts`
- `npm run build`

Continuity notes:

- retrieval-backed knowledge blocks are now visible on the same surfaces that already render citations/source blocks
- this round did not add new access control logic; the next active item is `P3-A-08`
