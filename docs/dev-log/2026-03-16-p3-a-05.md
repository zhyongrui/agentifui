# 2026-03-16 P3-A-05

Completed `P3-A-05` by adding shared `KnowledgeRetrievalQuery/Result` types, a lexical chunk-ranking helper, and `knowledgeService.buildRetrievalForUser(...)`.

Key wiring:

- `apps/gateway/src/routes/chat.ts`
  - builds retrieval before runtime invoke
  - persists `inputs.retrieval` onto the run
- `apps/gateway/src/services/workspace-runtime.ts`
  - runtime invocation input now accepts `retrieval`
  - structured placeholder runtime surfaces a grounding summary from it
- `apps/gateway/src/services/persistent-knowledge-service.ts`
  - ranks `knowledge_source_chunks` for `status = succeeded`
  - scope filter is `tenant` or active `groupId`

Validation:

- `npm run type-check`
- `npx vitest run apps/gateway/src/services/knowledge-retrieval.test.ts apps/gateway/src/services/workspace-runtime.test.ts apps/gateway/src/routes/chat.test.ts`
- `npm run build`

Continuity notes:

- retrieval is available to runtime adapters and persisted in run inputs, but not yet backfed into citations/source blocks
- next active item is `P3-A-06`
