# 2026-03-14

## P2-A5 Conversation Organization

Completed `P2-A5` by turning conversation history management into a real persisted capability across both `/chat` and `/chat/[conversationId]`.

Implemented in this round:

- shared contracts now include:
  - `WorkspaceConversation.pinned`
  - `WorkspaceConversationListItem.pinned`
  - `WorkspaceConversationUpdateRequest` / `WorkspaceConversationUpdateResponse`
- gateway and services now include:
  - `PUT /workspace/conversations/:conversationId`
  - in-memory and persistent workspace service support for `title`, `status`, and `pinned` updates
  - deleted conversations and their runs are filtered out of normal workspace reads
  - history ordering is now `pinned desc, updatedAt desc`
  - audit writes for `workspace.conversation.updated` and `workspace.conversation.deleted`
- web history/detail surfaces now include:
  - `/chat` actions for pin, rename, archive, restore, and delete
  - `/chat/[conversationId]` management panel for the same actions
  - archived conversations stay readable but disable composer, attachments, retry, regenerate, and suggested prompts
  - deleted conversations redirect back to `/chat?deleted=1`
- test coverage now includes:
  - unit and route coverage for conversation updates
  - persistence coverage across restart boundaries
  - browser coverage for `pin -> rename -> archive -> restore -> delete`

Useful continuity notes for future sessions:

- use `npm run test:e2e` on this host, not raw `npx playwright test`
  - the wrapper starts the expected project stack
  - direct Playwright launches can fail here with missing browser runtime libraries such as `libatk-1.0.so.0`
- Playwright response waits needed to be widened from `60s` to `120s`
  - shared Postgres load occasionally pushes auth requests past one minute
  - the helper lives in `tests/e2e/phase1-flows.spec.ts`
- the conversation-organization browser flow should seed a conversation directly in Postgres
  - this avoids consuming launch quota when the test only needs a persisted conversation shell
  - the helper is `seedWorkspaceConversation(email)` in `tests/e2e/phase1-flows.spec.ts`
- after `Restore`, do not assert the `Send message` button is enabled
  - the composer submit stays disabled while the draft is empty
  - assert the `Message` textarea and `Attachments` input are enabled instead
- stop-flow browser assertions should accept either `stopped` or `succeeded`
  - the stop request is still exercised
  - on fast completions, the run can naturally finish before the stop request wins the race
- earlier public-access/browser-testing context still applies
  - temporary external access can be provided with `cloudflared tunnel --url http://127.0.0.1:3112 --no-autoupdate`
  - long-lived `80/443` ingress remains the stable path for formal manual QA

Validation performed in this round:

- `npm run type-check`
- full `npm test`
- full `npm run test:e2e`

Final suite result after `P2-A5`:

- `21` unit/persistence test files passed
- `130` Vitest tests passed
- Playwright `12 passed`

Functional/browser verification summary:

- `PUT /workspace/conversations/:conversationId` now supports rename, pin/unpin, archive/restore and delete
- `/chat` keeps pinned conversations at the top and removes deleted conversations from normal history reads
- `/chat/[conversationId]` renders archived conversations as read-only and restores them without losing transcript/run history
- Playwright verified `pin -> rename -> archive -> restore -> delete`

Follow-up planning change:

- `P2-A5` is now complete
- the active next item in the rolling plan is `P2-A6`
- next priority is history search/filter enrichment by tag, attachment, feedback, and conversation status

## P2-A6 Structured Conversation History Filters

Completed `P2-A6` by turning `/chat` history search into a structured filter surface instead of a plain text query box.

Implemented in this round:

- shared contracts now include:
  - history filter query types for `status`, `attachment`, `feedback`, and `tag`
  - `WorkspaceConversationListItem.attachmentCount`
  - `WorkspaceConversationListItem.feedbackSummary`
- gateway and services now include:
  - `GET /workspace/conversations` parsing and validation for `status`, `attachment`, `feedback`, and `tag`
  - in-memory and persistent history filtering against app tags, attachment presence, assistant feedback, and archived/active state
  - persistent history now applies structured filters after the DB read and only then slices the final page limit
- web history UI now includes:
  - `/chat` filter controls for `Status`, `Tag`, `Attachments`, and `Feedback`
  - active-filter summary chips
  - conversation cards that display app tags, attachment counts, and feedback summaries
- test coverage now includes:
  - client serialization coverage for the new history query params
  - route coverage for filtered history results
  - persistence coverage proving the same filters survive an app restart
  - browser coverage for `Tag + Status + Attachments + Feedback` together

Useful continuity notes for future sessions:

- history `tag` filtering is based on the app catalog tag set, not custom conversation metadata
  - use `app_policy_watch` when you need a deterministic `policy` tag in browser tests
- persistent history intentionally filters in memory after the SQL read
  - SQL prefilters by `user_id`, `appId`, `groupId`, and `status`
  - final `limit` is applied after `tag/attachment/feedback/query` matching so the returned page stays dense
- if the top-level `npm test` wrapper is too quiet during persistence, split it manually
  - `npm run test:unit`
  - `npm run test:persistence`
  - keep those sequential; do not overlap them with Playwright on the shared Postgres DB
- the browser filter assertion should check both the active-filter badge and the conversation metadata chips
  - `4 active filters`
  - `1 attachments`
  - `Feedback +1 / -0`

Validation performed in this round:

- `npm run type-check`
- `npm run test:unit`
- `npm run test:persistence`
- `npm run test:e2e`

Final suite result after `P2-A6`:

- `22` Vitest test files passed
- `146` Vitest tests passed
- Playwright `13 passed`

Functional/browser verification summary:

- `/chat` now filters persisted history by app tag, attachment presence, assistant feedback state, and conversation status
- filtered conversation cards expose attachment and feedback summaries without reopening the thread
- the same structured filters survive a gateway restart on the persistent runtime
- Playwright verified that only the matching archived `Policy Watch` conversation remains after all four filters are active

Follow-up planning change:

- `P2-A6` is now complete
- the active next item in the rolling plan is `P2-B1`
- next priority is freezing the shared artifact DTO and wiring it into chat/run contracts

## P2-B1 Artifact Contract Freeze

Completed `P2-B1` by freezing a shared artifact DTO family and threading it through the existing chat, transcript, and run boundaries.

Implemented in this round:

- shared contracts now include:
  - `WorkspaceArtifact`
  - `WorkspaceArtifactSummary`
  - artifact `kind`, `source`, `status`, and payload variants for `markdown/text/json/table/link`
- gateway runtime now includes:
  - blocking chat responses that return `choices[0].message.artifacts`
  - streaming final chunks that return `artifacts`
  - persisted assistant transcript messages that keep artifact summaries
  - workspace run detail that exposes full artifacts through `run.artifacts`
- current bootstrap artifact behavior:
  - each assistant completion emits one draft markdown artifact derived from the assistant response body
  - `source = assistant_response`
  - `status = draft`
  - the full artifact currently lives in `runs.outputs.artifacts`
  - transcript messages only store the summary projection
- test coverage now includes:
  - client parsing for blocking and streaming artifact payloads
  - route coverage for blocking completions, streaming final chunks, and run detail
  - persistence coverage that proves artifacts survive restart on both the conversation and run surfaces

Useful continuity notes for future sessions:

- `P2-B1` intentionally does not create a dedicated artifact table
  - artifact storage is still piggybacking on `runs.outputs` and `conversations.inputs.messageHistory`
  - `P2-B2` should introduce the normalized persistence layer and then keep the DTO stable
- transcript messages should keep summaries only
  - full artifact payloads belong on run detail and chat gateway responses
  - this avoids bloating normal conversation reloads before preview/download routes exist
- the final SSE chunk is the artifact handoff point for streaming
  - earlier delta chunks remain content-only
  - browser/UI consumers should bind artifacts from the terminal chunk, then let the conversation refresh catch up

Validation performed in this round:

- `npx vitest run apps/web/src/lib/chat-client.test.ts apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/auth-persistence.test.ts`
- `npm run type-check`
- `npm run test:unit`

Functional verification summary:

- blocking chat completions now return artifact payloads alongside assistant text
- streaming completions now emit artifacts in the final SSE chunk
- workspace conversation reloads keep artifact summaries on assistant messages
- workspace run detail reloads keep full draft artifact payloads after restart

Follow-up planning change:

- `P2-B1` is now complete
- the active next item in the rolling plan is `P2-B2`
- next priority is normalizing artifact persistence and lookup beyond the current JSON boundary

## P2-B2 Artifact Persistence

Completed `P2-B2` by moving artifacts from a JSON-only convention into a dedicated persisted workspace table while keeping the external DTO stable.

Implemented in this round:

- database/storage now includes:
  - `workspace_artifacts` schema and migration
  - artifact metadata columns plus a kind-specific `payload` JSON blob
  - indexes on tenant, user, conversation, and run
- gateway persistence now includes:
  - run updates that sync `outputs.artifacts` into `workspace_artifacts`
  - run detail reads that prefer artifact rows from the new table
  - a fallback to `runs.outputs.artifacts` so pre-table data still renders
- workspace route surface now includes:
  - `GET /workspace/artifacts/:artifactId`
  - user-scoped ownership checks at the workspace boundary
- in-memory workspace service now mirrors the same behavior with artifact records per run
- test coverage now includes:
  - route coverage for artifact round-trips through `/workspace/artifacts/:artifactId`
  - persistence coverage for DB row creation plus route reads after restart

Useful continuity notes for future sessions:

- `P2-B2` keeps `runs.outputs.artifacts` populated on purpose
  - the dedicated table is now the preferred read source
  - the JSON copy remains as a backward-compatible fallback until preview/download flows fully migrate
- transcript messages still store artifact summaries in `messageHistory`
  - full artifact payloads belong on run detail and the artifact route
  - this preserves a lighter conversation reload path before preview pages exist
- the new migration makes persistence runs noticeably slower on the shared DB
  - the targeted `workspace + auth-persistence` vitest run took about 400s in this session
  - keep persistence and browser work strictly sequential

Validation performed in this round:

- `npx vitest run apps/gateway/src/routes/workspace.test.ts apps/gateway/src/routes/auth-persistence.test.ts`
- `npm run type-check`
- `npm run test:unit`

Functional verification summary:

- artifact records are now written into `workspace_artifacts` whenever a run persists artifact outputs
- `GET /workspace/artifacts/:artifactId` returns the persisted artifact payload for the owning user
- run detail still exposes artifacts after restart, now backed by the dedicated table
- persistence tests verified both the DB row and the route response for the same artifact id

Follow-up planning change:

- `P2-B2` is now complete
- the active next item in the rolling plan is `P2-B3`
- next priority is an artifact preview page wired from run/message entry points

## P2-B3 Artifact Preview Surface

Completed the `P2-B3` implementation pass by wiring persisted artifacts into the web workspace surface and adding a dedicated preview route.

Implemented in this round:

- web artifact UI now includes:
  - `WorkspaceArtifactLinkList` and `WorkspaceArtifactPreview` in `apps/web/src/components/workspace-artifacts.tsx`
  - assistant transcript artifact cards on `/chat/[conversationId]`
  - run replay artifact cards on the selected run detail panel
  - a dedicated preview page at `/chat/artifacts/[artifactId]`
- web client/runtime updates now include:
  - `fetchWorkspaceArtifact()` in `apps/web/src/lib/apps-client.ts`
  - `/chat/artifacts` added to protected-session routing
  - preview rendering for `markdown`, `text`, `json`, `table`, and `link` artifact payloads
- browser regression updates now include:
  - a seeded artifact preview flow in `tests/e2e/phase1-flows.spec.ts`
  - `seedWorkspaceArtifactConversation()` so the preview test can avoid workspace quota coupling
  - `expectAppsWorkspace()` now waits up to `60s` for the `/apps` redirect on this host

Useful continuity notes for future sessions:

- preview deep links intentionally live under `/chat/artifacts/[artifactId]`
  - this keeps `Chat history` active in the main nav and preserves a clear return path to the conversation surface
  - links carry `conversationId` and optional `runId` in the query string
- shared transcripts still do not expose clickable artifact previews
  - that is deliberate; `P2-B4` should decide how shared/read-only viewers are authorized for artifact access
- the artifact preview browser flow must not consume launch quota
  - seed the conversation and the `workspace_artifacts` row directly
  - otherwise earlier launch tests can push `Policy Watch` into `配额不足`
- this host is currently memory-pressured during Playwright
  - repeated `npm run test:e2e` attempts showed unrelated failures drifting between `browser.newContext()` setup timeouts and slow `/login -> /apps` redirects
  - those failures are environmental, not tied to the artifact preview route itself
  - in failing runs, the new artifact flow still reached `GET /workspace/artifacts/:artifactId -> 200`

Validation performed in this round:

- `npm run type-check`
- full `npm test`
- repeated `npm run test:e2e` wrapper runs

Validation summary:

- `type-check` passed
- `npm test` passed:
  - `21` unit test files passed
  - `133` unit tests passed
  - `1` persistence test file passed
  - `15` persistence tests passed
- `npm run test:e2e` is currently flaky on this host under memory/swap pressure
  - the code-specific artifact preview failures were fixed
  - the remaining failures drifted across unrelated existing browser tests after long host stalls

Functional verification summary:

- completed assistant messages now expose artifact cards from persisted message summaries
- selected run replay now exposes the same artifacts from persisted run detail
- `/chat/artifacts/[artifactId]` successfully resolves the persisted artifact route and renders the preview surface
- the preview page keeps a conversation return path through query params instead of forcing a history relaunch

Follow-up planning change:

- `P2-B3` implementation is complete
- the active next item in the rolling plan is `P2-B4`
- next priority is adding artifact download semantics, shared/read-only access rules, and audit coverage

## P2-B4 / P2-B5 Artifact Access Boundary And Audit

Completed `P2-B4` and `P2-B5` together by closing the owner/shared artifact access model and wiring artifact lifecycle events into the existing audit surface.

Implemented in this round:

- gateway/service surface now includes:
  - owner artifact download route:
    - `GET /workspace/artifacts/:artifactId/download`
  - shared/read-only artifact routes:
    - `GET /workspace/shares/:shareId/artifacts/:artifactId`
    - `GET /workspace/shares/:shareId/artifacts/:artifactId/download`
  - persistent and in-memory workspace services both now enforce:
    - owner reads by `artifact.user_id`
    - shared reads by active share membership on the parent conversation
- download semantics now serialize persisted artifacts as:
  - `markdown -> .md`
  - `text -> .txt`
  - `json -> .json`
  - `table -> .csv`
  - `link -> .txt`
- web/shared UX now includes:
  - shared transcript artifact cards on `/chat/shared/[shareId]`
  - artifact preview deep links that carry `shareId`
  - a preview-page `Download artifact` action backed by the same-origin gateway proxy
- audit surface now includes:
  - `workspace.artifact.generated`
  - `workspace.artifact.viewed`
  - `workspace.artifact.downloaded`
  - `artifact` as a first-class audit entity type

Useful continuity notes for future sessions:

- shared artifact preview intentionally reuses `/chat/artifacts/[artifactId]`
  - shared viewers are differentiated by `shareId` in the query string
  - this keeps one preview surface and one renderer path
- shared users must never call owner artifact routes directly
  - route tests now prove `/workspace/artifacts/:artifactId` returns `404` for a reader who only has share access
  - shared access must stay on `/workspace/shares/:shareId/artifacts/:artifactId`
- direct raw Playwright launches still need the prepared runtime libs on this host
  - `node scripts/prepare-playwright-runtime.mjs` returned:
    - `/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu`
  - targeted browser reruns should export that path via `LD_LIBRARY_PATH`
- repeated vitest retries left orphaned `node (vitest*)` workers alive on this host
  - `pgrep -af vitest` is the first thing to check when persistence reruns appear frozen
  - kill the stale workers before rerunning persistence or browser stages
- the affected persistence path was validated with a targeted run even though the full wrapper remained noisy on this machine
  - `apps/gateway/src/routes/auth-persistence.test.ts -t "persists chat completion run updates and keeps the trace id stable"`

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/web/src/lib/apps-client.test.ts`
- `npx vitest run apps/gateway/src/routes/workspace.test.ts`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists chat completion run updates and keeps the trace id stable"`
- `npm run test:unit`
- targeted Playwright:
  - `npx playwright test tests/e2e/phase1-flows.spec.ts --grep "conversation shares allow another group member to open a read-only shared transcript"`
  - with `LD_LIBRARY_PATH=/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu`

Validation summary:

- `type-check` passed
- `apps-client` unit tests passed: `14`
- `workspace routes` tests passed: `14`
- targeted persistent artifact/share regression passed
- `npm run test:unit` passed:
  - `21` files
  - `135` tests
- targeted Playwright shared-artifact browser flow passed:
  - `1 passed`

Functional/browser verification summary:

- owner-scoped artifact preview/download works through the persisted workspace artifact boundary
- shared transcript viewers can now open artifact previews without write access to the conversation
- shared transcript viewers can download the same artifact through the share boundary
- audit now records artifact generation, preview, and download with `accessScope` metadata for owner vs shared reads

Follow-up planning change:

- `P2-B4` is now complete
- `P2-B5` is now complete
- the active next item in the rolling plan is `P2-C1`
- next priority is freezing the HITL step contract and the pending-action persistence boundary

## P2-C1 / P2-C2 / P2-C3 HITL Contract, Read Route, And Response Persistence

Completed `P2-C1`, `P2-C2`, and `P2-C3` together by turning the placeholder HITL boundary into a real persisted workspace surface.

Implemented in this round:

- shared contracts now include:
  - `WorkspaceHitlStep`
  - `WorkspaceHitlStep.response`
  - `WorkspacePendingActionsResponse`
  - `WorkspacePendingActionRespondRequest`
  - `WorkspacePendingActionRespondResponse`
  - `WORKSPACE_ACTION_CONFLICT`
- gateway/service surface now includes:
  - `GET /workspace/conversations/:conversationId/pending-actions`
  - `POST /workspace/conversations/:conversationId/pending-actions/:stepId/respond`
  - in-memory and persistent workspace services that:
    - read the latest run-scoped HITL state
    - validate approval vs input-request action semantics
    - persist status transitions plus response payloads back into `runs.outputs.pendingActions`
- placeholder runtime behavior now includes:
  - `app_tenant_control` emitting `approval` steps for access-change style prompts
  - `app_tenant_control` emitting `input_request` steps for `details/form/justification/input` prompts
  - chat completion contracts exposing `pending_actions` on both blocking responses and the final SSE chunk
- client/runtime surface now includes:
  - `fetchWorkspacePendingActions()`
  - `respondToWorkspacePendingAction()`

Useful continuity notes for future sessions:

- `pending-actions` currently returns the full HITL state for the latest run, not just unresolved items
  - responded items stay in the array with updated `status` and `response`
  - this is intentional so `P2-C4` can render immediate post-submit state without opening run replay first
- the persistence bug I hit in this round was:
  - trying to update `runs.updated_at`
  - the `runs` table does not have that column
  - only `conversations.updated_at` should move on a HITL response write
- on this host, large `apply_patch` attempts against `apps/gateway/src/routes/workspace.ts` and `apps/web/src/app/(main)/chat/[conversationId]/page.tsx` can time out and truncate the file to zero bytes
  - if that happens, restore from `git show HEAD:...`
  - then reapply smaller patches
- `app_tenant_control` is now the deterministic placeholder app for HITL coverage
  - use it for route, persistence, and later browser tests instead of trying to coerce `app_policy_watch`

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/web/src/lib/apps-client.test.ts apps/gateway/src/routes/chat.test.ts apps/gateway/src/routes/workspace.test.ts`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists pending actions across app restarts for tenant control conversations"`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists pending action responses across app restarts"`

Validation summary:

- `type-check` passed
- `apps-client`, `chat routes`, and `workspace routes` targeted Vitest runs passed:
  - `42` tests passed across the three files
- targeted persistence coverage passed for:
  - pending-action read after restart
  - pending-action response persistence after restart

Functional verification summary:

- tenant-control completions now return typed HITL placeholder steps
- the latest run can be queried through the pending-actions route without opening run replay
- approval steps can be approved/rejected once and then lock
- input-request submissions now persist submitted values and actor metadata across app restarts

Follow-up planning change:

- `P2-C1` is now complete
- `P2-C2` is now complete
- `P2-C3` is now complete
- the active next item in the rolling plan is `P2-C4`
- next priority is rendering these pending-action states directly inside `/chat/[conversationId]` and closing browser coverage there

## P2-C4 In-Conversation HITL Cards

Completed `P2-C4` by rendering current pending-action state directly inside `/chat/[conversationId]` and closing the browser flow for submit + reload.

Implemented in this round:

- web conversation surface now includes:
  - pending-action cards above the transcript in `apps/web/src/app/(main)/chat/[conversationId]/page.tsx`
  - `approval` buttons plus `input_request` form controls
  - local draft state per pending-action field
  - in-place response summaries after submit / approve / reject
  - selected-run replay updates when the active run receives a new pending-action response
- persistent runtime fixes now include:
  - `runs.outputs.pendingActions` updates now use `jsonb_set(...)` instead of JSON concatenation
  - default `workspace_quota_limits` ids are now tenant-scoped so `grp_product` / `grp_research` no longer collide across tenants
- browser seed/runtime fixes now include:
  - `tests/e2e/phase1-flows.spec.ts` now seeds `jsonb` objects with `${object}::jsonb` instead of `JSON.stringify(... )::jsonb`
  - the new browser flow verifies:
    - pending-action card render
    - form submission
    - submitted values
    - reload persistence

Useful continuity notes for future sessions:

- `postgres` tagged templates treat `JSON.stringify(value) ::jsonb` as a JSON string scalar
  - pass the object directly (`${value}::jsonb`) or use `sql.json(value)`
  - otherwise later `jsonb_set(...)` updates fail with `cannot set path in scalar`
- if you are running raw Playwright against `next start` / `node dist/main.js`
  - rebuild and restart the local web/gateway stack after source edits
  - otherwise newly added routes can look missing because the browser is still hitting stale dist output
- quota seed ids must stay tenant-scoped on this project
  - `workspace_quota_limits.id` cannot just be `quota_group_grp_product`
  - group ids repeat across tenants, so cross-tenant browser flows will trip primary-key collisions
- persistence/browser verification on this host should stay sequential when the DB is shared
  - parallel `vitest` persistence runs can corrupt each other during schema reset
  - if a run appears frozen, check `pgrep -af vitest` before retrying

Validation performed in this round:

- `npm run type-check`
- `npx vitest run apps/web/src/lib/apps-client.test.ts apps/gateway/src/routes/workspace.test.ts apps/gateway/src/routes/chat.test.ts`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists pending action responses"`
- `npx vitest run apps/gateway/src/routes/auth-persistence.test.ts -t "persists platform tenant lifecycle and enforces suspended tenant access boundaries"`
- targeted Playwright:
  - `npx playwright test tests/e2e/phase1-flows.spec.ts --grep "conversation detail renders and submits pending HITL actions"`
  - with `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3211`
  - and `LD_LIBRARY_PATH=/home/bistu/zyr/pros/agentifui/.cache/playwright-runtime-libs/usr/lib/x86_64-linux-gnu`

Validation summary:

- `type-check` passed
- targeted unit/route tests passed:
  - `42` tests across `apps-client`, `workspace routes`, and `chat routes`
- targeted persistence tests passed:
  - pending-action response persistence after restart
  - cross-tenant platform lifecycle coverage with workspace quota seeding in the same run
- targeted browser regression passed:
  - `1 passed`

Functional/browser verification summary:

- `/chat/[conversationId]` now shows current HITL cards without opening run replay first
- input-request responses persist actor metadata and field values, then survive refresh
- the selected run detail stays aligned with the latest HITL response after submit
- multi-tenant workspace reads no longer fail while browser flows seed quota state in more than one tenant

Follow-up planning change:

- `P2-C4` is now complete
- the active next item in the rolling plan is `P2-C5`
- next priority is adding HITL audit coverage plus timeout / abandonment tracking
