# 2026-03-20 P4-H-09

## Goal
- close `P4-H-09` by adding a pre-release certification wrapper that runs the main release drills through one command

## Inputs Reviewed
- `package.json`
- `scripts/smoke-deploy.mjs`
- `scripts/backup-export.mjs`
- `scripts/backup-restore.mjs`
- `scripts/verify-restore-routes.mjs`
- `docs/guides/P4-H_QA_MATRIX.md`

## Implementation
- added `scripts/release-certification.mjs`
- added `npm run release:certify`
- the wrapper now orchestrates:
  - `eval:release`
  - `test:e2e:certification`
  - optional backup export / restore / route verification drills when the required env is present
- added `--print` and `--require-backup` modes so the certification plan can be inspected or made strict
- updated the QA matrix guide to treat `release:certify` as the default unified release drill
- marked `P4-H-09` complete in the execution plan

## Validation
- `node scripts/release-certification.mjs --print`

Observed results:

- the certification wrapper printed the expected step plan, including optional backup stages and their skip conditions

## Notes
- backup stages stay optional by default because they need environment-specific database and backup paths that are not always present on a fresh local session
