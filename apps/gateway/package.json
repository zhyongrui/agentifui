{
  "name": "@agentifui/gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/main.ts",
  "types": "./src/main.ts",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'gateway lint is not configured yet'"
  },
  "dependencies": {
    "@agentifui/db": "0.1.0",
    "@agentifui/shared": "0.1.0",
    "@fastify/cors": "^11.1.0",
    "better-auth": "^1.5.5",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.1",
    "pg": "^8.20.0",
    "pino-pretty": "^13.1.0"
  },
  "devDependencies": {
    "@types/pg": "^8.18.0"
  }
}
