global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/agentifui-alert-rules.yml

scrape_configs:
  - job_name: agentifui-gateway
    static_configs:
      - targets:
          - 127.0.0.1:4214
    metrics_path: /metrics
